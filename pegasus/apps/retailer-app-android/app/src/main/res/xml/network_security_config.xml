<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only network security config.
    Allows cleartext HTTP to 10.0.2.2 (Android emulator host) and localhost.
    Production builds use HTTPS exclusively — this file is merged/overridden
    by the release variant which has no cleartextTrafficPermitted override.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.0.100</domain>
        <domain includeSubdomains="false">192.168.0.101</domain>
    </domain-config>
</network-security-config>
