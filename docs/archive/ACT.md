# ACT Companion Protocol (Assess, Challenge, Transform)

Use this protocol for every technical task. The agent is a companion engineer, not a blind executor.

## Context Sync Log
- 2026-05-14: Phase-7 inventory freshness realtime landed additively: `suppliercoreroutes/routes.go` now receives `SupplierHub` + `WarehouseHub` deps, `suppliercoreroutes/imports.go` now emits websocket `INVENTORY_SYNC_COMPLETE` frames after successful `/v1/supplier/inventory/imports/{id}/apply` commits (session/rows/timestamp plus additive `warehouse_ids` and `product_ids`) to both supplier and affected warehouse channels, inventory apply summary payloads now return additive sync metadata, and inventory pages in `apps/admin-portal/app/supplier/inventory/page.tsx` + `apps/warehouse-portal/app/inventory/page.tsx` now consume the frame for immediate refresh, success toast, and 3-second row pulse feedback.
- 2026-05-14: Phase-2 supplier import sandbox compatibility landed additively: `schema/spanner.ddl` + `migrations/migrations.go` now provision `SupplierImportSessions`, interleaved `SupplierImportStagedRows`, and `SupplierImportMapping`; `suppliercoreroutes/imports.go` now provides supplier-scoped repository methods (`CreateImportSession`, `UpdateSessionStatus`, `SaveStagedRows`, mapping upsert) and mounts `/v1/supplier/inventory/imports` (`POST /`, `GET /{id}`, `POST /{id}/mapping`, `POST /{id}/approve`) with signed-upload tickets and no local file writes.
- 2026-05-14: Phase-3 signed-upload bridge landed additively: `suppliercoreroutes/imports.go` now creates deterministic GCS PUT tickets on `/v1/supplier/inventory/imports/` (`imports/{supplier_id}/{session_id}/raw.xlsx`) with `INITIALIZED` session writes and 50MB guardrails, `/v1/supplier/inventory/imports/{id}/uploaded` now transitions to `UPLOADED` and emits outbox `INVENTORY_IMPORT_UPLOADED` to Kafka topic `inventory.import.events`, and admin-portal `app/supplier/inventory/import/page.tsx` now performs drag-drop direct upload + uploaded-signal bridging with processing skeleton/scanner UX.
- 2026-05-14: Phase-4 AI mapping worker landed additively: `packages/ai-bridge` now provides a provider-agnostic `InventoryMapper` seam with Vertex Gemini token-counting support, `apps/ai-worker/import_worker.go` now consumes `inventory.import.events` (`INVENTORY_IMPORT_UPLOADED`) with bounded concurrency and streaming sample extraction (first 50 rows from GCS), persists `DISCOVERED` or `MAPPING_REQUIRED` plus mapping/anomaly summaries into `SupplierImportSessions` and `SupplierImportMapping`, and emits `INVENTORY_IMPORT_STATUS_UPDATE` so `kafka/notification_dispatcher.go` can push supplier websocket `IMPORT_STATUS` frames.
- 2026-05-14: Phase-6 atomic apply landed additively: `suppliercoreroutes/imports.go` now mounts `POST /v1/supplier/inventory/imports/{id}/apply` with idempotency wrapping, supplier-ownership enforcement, and `APPROVED|APPLYING` state gating; `ApplyImportSession` now performs one transactional apply pass that filters approved staged rows, upserts staged `is_new_product` catalog entries into `SupplierProducts`, dual-writes quantities into `SupplierInventory` and `SupplierInventoryV2`, writes `InventoryAuditLog` journal rows with reason `BULK_IMPORT`, finalizes sessions to `APPLIED` (idempotent on re-entry), and marks session `FAILED` with `error_summary` on non-conflict failures; admin-portal finalize flow in `app/inventory/import/page.tsx` now calls `/apply`.
- 2026-05-14: Supplier inventory import staging landed: `schema/spanner.ddl` + `migrations/migrations.go` now provision `InventoryImportSessions` and interleaved `InventoryImportRows`; `supplier/imports.go` now persists create/list/detail/rows/mapping/approve/apply/status flows with supplier-scope and warehouse-scope guards; `apps/admin-portal/app/supplier/inventory/import/page.tsx` now ships the supplier bulk-import wizard shell with inventory-page entrypoint linkage.
- 2026-05-14: Retailer Android background-push parity landed: `apps/retailer-app-android/.../PegasusFirebaseMessagingService.kt` now maps `SETTLEMENT_REQUIRED` and `DELIVERY_SESSION_UPDATED` to explicit fallback notification titles/bodies so settlement/session-update alerts remain user-visible when websocket delivery is unavailable.
- 2026-05-14: Retailer inbox parity hardening landed: retailer desktop notification refresh now includes `SETTLEMENT_REQUIRED` + `DELIVERY_SESSION_UPDATED`, iOS/Android notification inbox icon mappings now include reassignment/settlement/session-update event types, and shared `packages/types/ws-events.ts` now includes additive retailer WS contracts (`ORDER_REASSIGNED`, `SETTLEMENT_REQUIRED`, `DELIVERY_SESSION_UPDATED`, `CART_SYNC_UPDATED`) for cross-surface type parity.
- 2026-05-14: Delivery dispute settlement path closed: `treasury/settlement.go#HandleInvoiceStatusOverride` now emits transactional-outbox `DELIVERY_DISPUTED` when invoice custody transitions into `DISPUTED` (with additive order/session/recipient context), and `kafka/notification_dispatcher.go` now consumes `DELIVERY_DISPUTED` for supplier inbox/websocket review fanout. Dispatcher delivery now stays websocket-capable when Spanner persistence dependency is absent, and focused tests now cover retailer websocket reception for `DELIVERY_SESSION_UPDATED` and active-fulfillment SQL state alias compatibility.
- 2026-05-14: Dynamic delivery settlement drift closure landed: `kafka/notification_dispatcher.go` now consumes `DELIVERY_SESSION_UPDATED`, projects additive settlement/session numeric fields (`amount`, `original_amount`, `adjusted_amount`, `fee_*`, currency/session/invoice ids) into realtime websocket frames for retailer payment consumers, and keeps `SETTLEMENT_REQUIRED` websocket payload shape aligned with modal consumers; `order/service.go#ActiveFulfillments` now includes `AWAITING_PAYMENT` with legacy `AWAITING_GLOBAL_PAYNT` compatibility to prevent active-payment rows from dropping off retailer fulfillment surfaces.
- 2026-05-13: Dynamic delivery handshake tranche landed: `schema/spanner.ddl` + `migrations/migrations.go` now provision additive `DeliverySessions` and immutable `DeliverySessionAdjustments` (with supplier-scoped indexes), `order/delivery_handshake.go` introduces `VerifyHandshake` and `UpdateOrderDuringDelivery` flows (JWT/plain-token compatibility, geofence gate, reconciliation audit writes, and outbox `DELIVERY_SESSION_UPDATED`), `deliveryroutes/routes.go` now mounts `POST /v1/delivery/{verify-handshake,update-order-during-delivery}` for DRIVER callers with claim-bound driver scope, `order/service.go#ConfirmOffload` now transitions the session to `SETTLEMENT_AWAIT` and emits `SETTLEMENT_REQUIRED`, `order/service.go#CompleteOrder` now blocks completion while delivery-session settlement remains uncleared, and both `payment/session.go#SettleSession` plus `order/service.go#CollectCash` now advance matching sessions to `FINAL_SETTLEMENT` with `PaymentClearedAt`; retailer websocket fanout now emits additive `SETTLEMENT_REQUIRED` alongside legacy `PAYMENT_REQUIRED` for compatibility.
- 2026-05-13: Retailer role-row settlement client parity landed: retailer desktop `apps/retailer-app-desktop/components/PaymentModal.tsx` now consumes `SETTLEMENT_REQUIRED` and applies `DELIVERY_SESSION_UPDATED` amount deltas in-modal, retailer iOS `apps/retailer-app-ios/retailerapp/retailerapp/Services/RetailerWebSocket.swift` now maps `SETTLEMENT_REQUIRED` into the payment-required stream and treats `DELIVERY_SESSION_UPDATED` as order-refresh signal parity, and retailer Android websocket and view-model consumers (`data/api/RetailerWebSocket.kt`, `ui/navigation/NavigationViewModel.kt`, `ui/screens/orders/OrdersViewModel.kt`) now consume both events for payment-state and order-refresh convergence.
- 2026-05-13: S-level dynamic billing metering tranche landed: `schema/spanner.ddl` + `migrations/migrations.go` now provision `BillingMeterEvents`, `BillingSupplierMeters`, and `BillingGlobalMeters` (sharded) for idempotent `ORDER_FINALIZED` metering; `kafka/billing_tier_worker.go` now consumes `ORDER_FINALIZED` through `internal/services/billing/meter_worker.go`, updates meters idempotently by `OrderId`, applies milestone-driven fee updates by writing `SystemConfig.platform_fee_basis_points` (`billing_*` milestone keys), and emits additive `FEE_RATE_ADJUSTED` via transactional outbox on threshold crossings; payout math now resolves runtime basis points (`settings/platform_config.go`, `order/service.go`, `kafka/treasurer.go`, `analytics/earnings.go`), and `/v1/treasury/ledger` plus `admin-portal/app/treasury/page.tsx` now expose/render additive billing history + fee-milestone telemetry.
- 2026-05-13: Checkout warehouse selection deterministic tie-break tranche landed: `proximity/warehouse_resolver.go` now ranks candidates by H3 grid distance and applies Redis-backed round-robin (`wh:rr:<supplierId>:<retailerCell>`) across equal-ring candidates, with deterministic lexical fallback when Redis is unavailable, across both grid-cell and Spanner fallback resolver paths to avoid dense-cell hotspotting.
- 2026-05-13: Autonomous routing fallback tranche landed: `factory/network_optimizer.go#SelectOptimalFactoryWithTelemetry` now falls back to nearest active supplier factory when no active `SupplyLanes` candidate exists, applies additive `Factories.ProductTypes` product-aware preference when explicit mappings are present, and falls back to warehouse `PrimaryFactoryId`/`SecondaryFactoryId` when coordinates are unavailable; look-ahead and pull-matrix skip logs now report missing routing candidates instead of lane-only misses.
- 2026-05-13: InventoryV2 read-convergence tranche landed: replenishment and planning reads now resolve `SupplierInventoryV2` first with legacy fallback (`replenishment/engine.go#getWarehouseStock`, `factory/look_ahead.go#fetchWarehouseInventory`, `factory/predictive_push.go` projected-breach warehouse scan, and `warehouse/dashboard.go` low-stock KPI query).
- 2026-05-13: InventoryV2 runtime activation tranche landed: supplier and warehouse inventory APIs now prefer warehouse-scoped `SupplierInventoryV2` reads with `SupplierInventory` fallback, unified checkout now mirrors effective stock decrements into `SupplierInventoryV2` for warehouse-assigned plans, and supplier/factory restock paths (`supplier/{reconcile,returns,vetting}` and factory transfer receive flows) now mirror additive stock restores/increments into `SupplierInventoryV2` when `warehouse_id` context is present.
- 2026-05-13: Settlement-locality bootstrap tranche landed: `schema/spanner.ddl` + `migrations/migrations.go` now add `MasterInvoices.SettlementTarget`, `Warehouses.PaymentConfigId`, and `SupplierInventoryV2` (`SupplierId,WarehouseId,ProductId` key with `H3Cell`), unified/card/cash checkout invoice writes now persist settlement targets (`GLOBAL_SUPPLIER`/`LOCAL_WAREHOUSE`/`MIXED_WAREHOUSE`), supplier warehouse CRUD now exposes additive `payment_config_id`, warehouse ops payment-config reads prefer warehouse-scoped configs with supplier-default fallback, and vault order credential resolution now applies precedence `Warehouses.PaymentConfigId -> SupplierPaymentConfigs.WarehouseId -> supplier default`.
- 2026-05-13: Adyen gateway rollout landed: `payment/provider.go` now resolves `ADYEN` to an official SDK-backed adapter (`payment/adyen.go`) for hosted checkout (Payment Links), `order/service.go` card checkout now binds Adyen checkout URLs through `SessionService.BindProviderCheckout`, `/v1/webhooks/adyen` is mounted in `webhookroutes` and handled by `payment/adyen_webhook.go` with signature-first HMAC verification + idempotency guard, and direct Adyen refund/charge operations now return `ErrAdyenDirectOperationUnsupported` with refund service fallback to manual-required status.
- 2026-05-13: Warehouse region-filter continuation landed beyond analytics/treasury: `auth.AppendRegionFilter` now scopes warehouse read models for `/v1/warehouse/ops/{dashboard,orders,orders/{id},dispatch/preview,crm,returns,financials}` plus demand-forecast order-side quantity/burn-rate reads on `/v1/warehouse/demand/forecast` through `Retailers.RegionId` joins when `region_id` scope is active.
- 2026-05-13: Warehouse region-filter read adoption landed: `auth.AppendRegionFilter` now scopes warehouse read models for `GET /v1/warehouse/ops/analytics` and `GET /v1/warehouse/ops/treasury` (`overview` + `invoices`) through `Retailers.RegionId` joins when `region_id` scope is active.
- 2026-05-13: Webhook + region-filter query continuation landed: `webhookroutes` now mounts both `POST /v1/webhooks/global-pay` and `POST /v1/webhooks/stripe` through priority/log middleware, and `auth.AppendRegionFilter` is now applied on supplier-scoped read paths for `/v1/treasury/{cash-holdings,supplier/settlement-report}` plus `/v1/supplier/picking-manifests*` via `Retailers.RegionId` when `region_id` scope is active.
- 2026-05-12: Provider-execution + region-scope tranche landed: payment session create/attempt/bind/retry paths now normalize and validate gateways through `payment.NewProviderClient`, checkout URL builders fail closed for unsupported/unavailable providers, Global Pay webhook execution validates session gateway through the provider resolver, unified checkout/card checkout now explicitly fail closed when `ADYEN` is selected before side effects, and supplier/warehouse role-row route composers now inject `auth.RequireRegionScopeWithClient` so `region_id` query filters are scope-validated for SUPPLIER/ADMIN/WAREHOUSE callers.
- 2026-05-12: Phase-1 global modular substrate landed: Spanner `Regions` + `RegionalConfigs` tables were added with additive nullable `RegionId` links on `Suppliers`, `Warehouses`, and `Retailers`; `countrycfg/regions.go` now resolves supplier-region/default-region plus regional overrides and `bootstrap.NewApp` seeds `UZ-DEFAULT`; payment gateway resolution now routes through `payment.NewProviderClient` (`GLOBAL_PAY`, `ADYEN` fail-closed, `CASH`) and supplier gateway capability metadata now includes `ADYEN` onboarding fields.
- 2026-05-12: Supplier/factory/driver multitenant hardening landed: supplier and warehouse driver create flows plus factory staff create/register now enforce duplicate-phone checks within supplier scope (`SupplierId`) instead of global phone collisions; `/v1/auth/driver/login` and `/v1/auth/factory/login` now disambiguate same-phone accounts with optional `supplier_id`/`factory_id` and reject ambiguous multi-match logins; `/v1/delivery/sms-complete` now validates `driver_phone` against the order-assigned driver record instead of global phone lookup; supplier warehouse driver-count aggregates now include explicit supplier predicates.
- 2026-05-12: Warehouse treasury invoice currency parity landed: `warehouse/treasury.go` now returns additive per-invoice `currency` plus `amount_uzs` alias compatibility on `/v1/warehouse/ops/treasury?view=invoices`, and warehouse portal, iOS, and Android treasury invoice views now render invoice currency instead of hardcoded `UZS`.
- 2026-05-12: Treasury/anomaly read-model currency parity landed: `/v1/admin/reconciliation` now returns additive `currency` with `UZS` fallback for legacy rows, `/v1/treasury/cash-holdings` now returns additive report+row `currency` (default `UZS`), and admin-portal reconciliation/treasury pages now consume additive currency fields while preserving legacy `amount_uzs` compatibility paths.
- 2026-05-12: Refund and chargeback currency hardening landed: `payment/refund.go` now accepts additive `amount`/`currency` inputs (while preserving `amount_uzs`), validates against settled session currency, and returns additive `amount` + `currency` fields; `payment/chargeback.go` and `paymentroutes/routes.go` now accept additive `amount`/`currency` input and persist anomaly rows using normalized runtime currency instead of hardcoded UZS.
- 2026-05-12: Checkout currency context slice landed: unified checkout now resolves supplier-effective currency via `countrycfg.Service` for `MasterInvoices`, `Orders`, `OrderLineItems`, and outbox payload currency fields with a temporary 422 guard on mixed-supplier currencies per invoice; payment sessions now honor `CreateSessionRequest.Currency` and preserve existing session currency across retry gateway switches.
- 2026-05-12: Supplier reassignment parity closed: `ORDER_REASSIGNED` outbox payloads now include additive `supplier_id`, dispatcher fanout resolves supplier recipients via payload + order lookup fallback, and admin-portal supplier dashboard/orders/fleet views now consume `ORDER_REASSIGNED` hybrid invalidation for immediate refresh.
- 2026-05-12: Retailer AI confirmation mutations now emit `AI_ORDER_CONFIRMED`/`AI_ORDER_REJECTED` through transactional outbox inside the same `Orders` `ReadWriteTransaction`, and `ORDER_REASSIGNED` notification fanout now resolves affected retailer recipients for desktop/iOS/Android realtime refresh parity.
- 2026-05-12: Retailer cart realtime convergence landed: `POST /v1/retailer/cart/sync` now emits `CART_SYNC_UPDATED` over `ws/retailer` after commit, and retailer desktop, iOS, and Android cart stores now rehydrate via `GET /v1/retailer/cart/sync` on that signal.
- 2026-05-10: Closed production wiring drift for checkout and driver availability events: unified checkout now emits `ORDER_VALIDATION_FAILED`, `PAYMENT_CLEARED`, and `ORDER_FINALIZED` through canonical `kafka.TopicMain`, and `/v1/driver/availability` writes `DRIVER_AVAILABILITY_CHANGED` via transactional outbox before best-effort notification fanout.
- 2026-05-10: Added additive telemetry durability behind `/ws/telemetry`: `telemetry/hub.go` now derives or forwards `trace_id`, enqueues best-effort journal events to Kafka topic `pegasus-telemetry-raw` via `telemetryaudit/`, and `telemetryaudit.StartSink` persists replay-safe audit rows into `DriverTelemetry` without changing the JSON WebSocket contract current admin/mobile clients use.
- 2026-05-07: Completed tranche-1 cache infra slog migration by converting `cache/{redis,pubsub,invalidate,middleware,warehouse_load,warehouse_geo,priority,circuitbreaker}.go` from `log.*` to structured `slog`.
- 2026-05-07: Completed low-touch infra slog sweep by migrating `kafka/dlq.go` and `factory/replenishment_lock.go` from `log.*` to structured `slog` with no behavior changes.
- 2026-05-07: Closed Phase VIII replenishment producer gap by wiring outbox producers for `STOCK_THRESHOLD_BREACH` (`factory/pull_matrix.go`) and `LOOK_AHEAD_COMPLETED` (`factory/look_ahead.go`); synced event contract comments plus architecture/inventory context docs.
- 2026-05-07: Synced runtime/docs after replenishment pipeline + logging tranche. Added notification consumers for `REPLENISHMENT_LOCK_ACQUIRED`, `REPLENISHMENT_LOCK_RELEASED`, `STOCK_THRESHOLD_BREACH`, and `LOOK_AHEAD_COMPLETED` in `kafka/notification_dispatcher.go`; migrated high-noise runtime logging to `slog` across `analytics/`, `sync/`, `replenishment/`, `treasury/`, `vault/`; refreshed known-gap status in `.github/copilot-instructions.md` and `.github/gemini-instructions.md`.

## Scope (Always On)
Apply ACT for all technical asks, including:
- feature implementation
- bug fixes
- refactors
- architecture changes
- plan reviews and task decomposition
- reliability/performance audits
- migration and cutover planning

## A: Assess
1. Parse user intent, constraints, and risk tolerance.
2. Run targeted code retrieval before action.
   Preferred path (workspace tooling):
   - `file_search` or `grep_search` to find the entry point
   - `read_file` for the owning implementation and nearest dependency boundary
   - `vscode_listCodeUsages` or `semantic_search` when symbol blast radius matters
   Fallback path only if workspace retrieval tools are insufficient:
   - terminal `rg` searches scoped to the relevant package or surface
3. For every non-trivial technical task, plan review, architecture decision, audit, debugging session, migration, or multi-step implementation, run `sequential_thinking` from the `sequential-thinking` MCP server before editing or finalizing an answer. The canonical local entrypoint is `.agents/extensions/sequential-thinking/mcp-server.mjs`. Use revisions and branches when evidence changes the plan or viable alternatives need comparison, and summarize only decisions/risks/work in user-facing replies.
4. Read required architecture docs before edits:
   - `pegasus/context/architecture.md`
   - `pegasus/context/architecture-graph.json`
   - `pegasus/context/technology-inventory.md`
   - `pegasus/context/technology-inventory.json`
   - `pegasus/context/design-system.md`
   - `pegasus/context/purpose.md`
   - `pegasus/context/ui-design.md` for every UI-affecting task (web, desktop, Android, iOS, Expo)
5. Apply codebase-first weighting: primary context must come from real runtime code (definitions, usages, graph). Docs are mandatory verification, not a substitute for code retrieval.
6. Identify blast radius across API, mobile, web, workers, and infra.
7. UI gate (mandatory for UI-affecting work): before editing any user-facing surface, enumerate the backend endpoint/event/DTO feeding the screen, the frontend data layer that maps it, and every client in the role row that also consumes the feature. Do not treat a single web page or app screen as complete context.
8. Warehouse live gate: treat `/ws/warehouse` plus the supply-request and dispatch-lock DTOs as one contract across warehouse portal, warehouse iOS, and warehouse Android. Do not change one consumer without checking the other two.
9. Warehouse live resilience gate: `/ws/warehouse` consumers must auto-reconnect and surface stale/offline state. A silent frozen live view does not count as complete.
10. Supplier geo-planning gate: treat `/v1/supplier/serving-warehouse`, `/v1/supplier/geo-report`, `/v1/supplier/zone-preview`, `/v1/supplier/warehouses/validate-coverage`, and `/v1/supplier/warehouse-loads` as one contract owned by `pegasus/apps/backend-go/proximityroutes/routes.go`; check the supplier portal coverage map and warehouse coverage-editor consumers before changing one path.
11. Supplier self-service gate: treat `/v1/supplier/configure`, `/v1/supplier/billing/setup`, `/v1/supplier/profile`, `/v1/supplier/shift`, `/v1/supplier/payment-config`, `/v1/supplier/gateway-onboarding`, and `/v1/supplier/payment/recipient/register` as one contract owned by `pegasus/apps/backend-go/supplierroutes/routes.go`; check the supplier profile, payment-config, setup-billing, and shift consumers before changing the route family.
12. Supplier warehouse-ops gate: treat `/v1/supplier/org/members*`, `/v1/supplier/staff/payloader*`, `/v1/supplier/warehouse-staff*`, `/v1/supplier/warehouses*`, `/v1/supplier/warehouses/{id}/coverage`, and `/v1/supplier/warehouse-inflight-vu` as one contract owned by `pegasus/apps/backend-go/supplierroutes/routes.go`; check the supplier org, staff, warehouses, coverage-editor, and factory-network-map consumers before changing the route family.
13. Supplier catalog-pricing gate: treat `/v1/supplier/products*`, `/v1/supplier/products/upload-ticket`, `/v1/supplier/pricing/rules*`, and `/v1/supplier/pricing/retailer-overrides*` as one contract owned by `pegasus/apps/backend-go/suppliercatalogroutes/routes.go`; check the supplier products, catalog, pricing, and retailer-overrides portal consumers before changing one path.
14. Supplier logistics gate: treat `/v1/supplier/picking-manifests*`, `/v1/supplier/manifests*`, `/v1/payload/manifest-exception`, `/v1/supplier/manifest-exceptions`, `/v1/supplier/fleet-volumetrics`, `/v1/supplier/dispatch-queue`, and `/v1/supplier/dispatch-preview` as one contract owned by `pegasus/apps/backend-go/supplierlogisticsroutes/routes.go`; check the supplier manifests, dispatch, orders, and payload-facing manifest exception consumers before changing one route.
15. Supplier insights gate: treat `/v1/supplier/country-overrides*`, `/v1/supplier/analytics/*`, `/v1/supplier/financials`, and `/v1/supplier/crm/retailers*` as one contract owned by `pegasus/apps/backend-go/supplierinsightsroutes/routes.go`; supplier CRM list/detail payloads are expected to carry retailer contact email additively for portal drawer consumers, so check the supplier country-overrides, analytics, dashboard, demand, and CRM consumers before changing one path.
16. Supplier operations gate: treat `/v1/supplier/fleet/*`, `/v1/supplier/fulfillment/pay`, `/v1/supplier/returns*`, `/v1/supplier/quarantine-stock`, and `/v1/inventory/reconcile-returns` as one contract owned by `pegasus/apps/backend-go/supplieroperationsroutes/routes.go`; check the supplier fleet, returns, depot-reconciliation, and supplier-side fulfillment consumers before changing one path.
17. Supplier planning gate: treat `/v1/supplier/delivery-zones*`, `/v1/supplier/factories*`, `/v1/supplier/geocode/reverse`, `/v1/supplier/retailers/locations`, `/v1/supplier/supply-lanes*`, `/v1/supplier/network-{mode,analytics}`, `/v1/supplier/replenishment/{kill-switch,audit,pull-matrix,predictive-push}`, and `/v1/supplier/warehouses/{territory-preview,apply-territory}` as one contract owned by `pegasus/apps/backend-go/supplierplanningroutes/routes.go`; supplier factory create/list/detail/profile payloads are expected to carry additive `h3_index` and `product_types` metadata for the admin-portal planning consumers, and supply-lane mutate actions must resolve the organisation-level supplier scope via `claims.ResolveSupplierID()` while honoring the published `PATCH /v1/supplier/supply-lanes/{id}` contract, so check the supplier map, factory-planning, territory, supply-lane, and replenishment-control consumers before changing one path.
18. Supplier core gate: treat `/v1/supplier/dashboard`, `/v1/supplier/earnings`, `/v1/supplier/inventory*`, and `/v1/supplier/orders*` as one contract owned by `pegasus/apps/backend-go/suppliercoreroutes/routes.go`; supplier inventory is expected to honor the mounted root `PATCH /v1/supplier/inventory` contract and publish additive `sku_id`/`product_name` aliases for portal consumers, so check the supplier dashboard, earnings, inventory, and order-vetting consumers before changing one path.
19. Retailer role-row gate: treat `/v1/retailer/analytics/*`, `/v1/orders/request-cancel`, `/v1/order/{create,cancel,cash-checkout,card-checkout}`, `/v1/retailer/shop-closed-response`, `/v1/retailer/family-members*`, `/v1/retailer/orders/{confirm-ai,reject-ai}`, `/v1/orders/{edit-preorder,confirm-preorder}`, `/v1/retailer/cart/sync`, `/v1/retailer/suppliers*`, `/v1/retailer/profile`, `/v1/retailers/{retailerID}/orders`, `/v1/retailer/{tracking,cards,pending-payments,active-fulfillment}`, `/v1/retailer/card/{initiate,confirm,deactivate,default}`, `/v1/retailer/settings/auto-order*`, and `/v1/ws/retailer` as one contract owned by `pegasus/apps/backend-go/retailerroutes/routes.go`; treat shared `/v1/orders*` and `/v1/order/refunds` as the adjacent compatibility contract owned by `pegasus/apps/backend-go/orderroutes/routes.go`; check retailer desktop, retailer iOS, and retailer Android consumers together before changing one path because those clients already depend on overlapping but not identical slices of this route family.
20. Simulation harness gate: treat `/v1/internal/sim/{start,stop,status}` as one contract owned by `pegasus/apps/backend-go/simroutes/routes.go`; keep role guard ADMIN-only, preserve boot-time `SIMULATION_ENABLED` gating through `app.Simulation`, and keep all handler logic in `pegasus/apps/backend-go/simulation/handler.go`.
21. Driver role-row gate: treat `/v1/driver/{earnings,history,availability,pending-collections,profile,manifest-gate,manifest}`, `/v1/fleet/manifest` (legacy alias), and `/v1/ws/driver` as one contract owned by `pegasus/apps/backend-go/driverroutes/routes.go`; treat shared order compatibility plus execution routes in `orderroutes` (including `/v1/order/{deliver,validate-qr,confirm-offload,complete,collect-cash,refund,amend}` and `/v1/vehicle/*`) together with adjacent `/v1/delivery/arrive` handling as the same mobile role-row contract, check both `driver-app-android` and `driverappios` consumers before changing one path, keep high-consequence driver mutations (`/v1/order/deliver`, `/v1/order/confirm-offload`, `/v1/order/complete`, `/v1/order/collect-cash`, `/v1/delivery/arrive`) on deterministic `Idempotency-Key` headers across both clients, and keep `/v1/driver/profile` / driver-login payloads additive for dual-node operations (`home_node_type`, `home_node_id`, `driver_mode`, and factory-node metadata) so one app pair can cover factory-transfer and warehouse-delivery drivers without app forks.
22. Payload role-row gate: treat `/v1/payloader/{trucks,orders,recommend-reassign}`, `/v1/payload/seal`, and `/v1/ws/payloader` as one contract owned by `pegasus/apps/backend-go/payloaderroutes/routes.go`; treat shared manifest lifecycle paths in `supplierlogisticsroutes`, `POST /v1/delivery/missing-items` in `deliveryroutes` (driver+payloader; additive payload compatibility for `items` alias and `source=PAYLOAD_TERMINAL` empty-item flags), reassign in `fleetroutes`, and inbox/token paths in `userroutes` as adjacent payload contracts, and check payload-terminal, payload-app-ios, and payload-app-android consumers together before changing one path.

## C: Challenge
If prompt/plan is unsafe, incomplete, or likely to break production, do not execute it as-is.
Provide a corrected plan and explain why.

Mandatory production-safety checks:
1. Spanner: index-backed reads, `ReadWriteTransaction` for mutations, version gating.
2. Kafka: transactional outbox for state changes, partition key by aggregate root, consumer idempotency.
3. Redis: invalidate after commit, Pub/Sub fan-out parity, no stale local cache assumptions.
4. Terraform: infra contract updates in `pegasus/infra/terraform` when topics/tables/networking change.
5. Maglev: stateless pod assumptions, consistent hashing compatibility, no sticky-session dependencies.
6. Hyper-scale: design for 10M-request class traffic with priority guard, rate limiting, circuit breaker, bounded workers.

Prompt verification gate (mandatory before implementation):
1. Classify prompt risk: `safe`, `risky`, `production-breaking`, or `scope-conflict`.
2. If class is not `safe`, respond first with:
   - what is wrong in the current prompt/plan,
   - production impact across Spanner/Kafka/Redis/Terraform/Maglev,
   - a safer execution plan.
3. Execute only the safer plan. Never implement a known production-breaking plan as requested.

## T: Transform
Transform weak plans into safe, phased execution:
1. Local baseline first: run with `pegasus/docker-compose.yml` and validate health.
2. Dual-sync updates: code + docs + architecture graph in same change set.
3. Contract validation: API shapes, Kafka payload consumers, cache keys, and role scopes.
4. Frontend-context validation for UI work: verify the backend contract is represented across every affected client surface for the role (web, desktop, Android, iOS, Expo as applicable), or explicitly gate hidden clients behind a rollout flag.
5. Cutover readiness: keep local code production-compatible so real server migration is wiring/config only.
6. Rollback readiness: additive schema and event changes, version-safe clients, and explicit rollback path.
7. Plan-anchor continuity: start with one stable plan anchor (`P1`, `P2`, ...), preserve IDs across all phases, and avoid creating detached phase-only tasks.
8. Phase handoff integrity: when phased execution is used, every phase exit must include remaining work and blockers mapped to plan-anchor IDs.
9. Final reconciliation: completion output must map each plan-anchor ID to `completed`, `deferred`, or `blocked`, and next steps must list unresolved plan-anchor IDs before optional extras.

## One-Eye Guard Suite (Mandatory On PR)
Run and pass all six guard scripts for pull requests:
1. `python3 pegasus/scripts/contract_guard_mcp.py --repo-root . --base-sha <base> --head-sha <head>`
2. `python3 pegasus/scripts/architecture_guard_mcp.py --repo-root . --base-sha <base> --head-sha <head>`
3. `python3 pegasus/scripts/design_system_guard_mcp.py --repo-root . --base-sha <base> --head-sha <head>`
4. `python3 pegasus/scripts/production_safety_guard.py --repo-root . --base-sha <base> --head-sha <head>`
5. `python3 pegasus/scripts/visual_test_intelligence_guard.py --repo-root . --base-sha <base> --head-sha <head>`
6. `python3 pegasus/scripts/security_guard.py --repo-root . --base-sha <base> --head-sha <head>`

`contract_guard_mcp.py`, `architecture_guard_mcp.py`, and `design_system_guard_mcp.py` enforce codebase-first MCP context discipline: trigger-scoped PRs must include stronger real-codebase coverage than context-doc coverage.

## Sync-On-Change Contract
After every execution that changes architecture, integrations, dependencies, or operational behavior, update all relevant files in one change set:
1. `.github/ACT.md`
2. `.github/copilot-instructions.md`
3. `.github/gemini-instructions.md`
4. `pegasus/context/architecture.md`
5. `pegasus/context/architecture-graph.json`
6. `pegasus/context/technology-inventory.md`
7. `pegasus/context/technology-inventory.json`

## Skills to Invoke
Use relevant skills when risk is present:
- `gap-hunter`
- `maglev`
- `kafka-event-contracts`
- `spanner-discipline`
- `cache-redis-correctness`
- `test-with-spanner`

## Companion Rule
If the user-proposed plan is wrong, propose a better technical approach first, then execute the safer plan.
Do not silently follow a plan that compromises production safety.
