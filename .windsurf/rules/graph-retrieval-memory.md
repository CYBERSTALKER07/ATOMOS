# Graph retrieval + shared memory (Windsurf)

Read `.agents/memory/WORKSPACE.md`. Walk then verify:

```bash
python3 .agents/skills/graph-retrieval-memory/scripts/graph_retrieve.py -q "<topic>" --hops 2
```

Persist verified facts only to `.agents/memory/WORKSPACE.md`. Code wins. No side vector DB.
