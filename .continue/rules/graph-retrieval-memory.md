# Graph retrieval + shared memory (Continue)

Read `.agents/memory/WORKSPACE.md`.

```bash
python3 .agents/skills/graph-retrieval-memory/scripts/graph_retrieve.py -q "<topic>" --hops 2
```

Open hits. Persist verified only. See `.agents/skills/graph-retrieval-memory/references/always-on.md`.
