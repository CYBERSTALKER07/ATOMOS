# pegasusX Technology Inventory

> [!NOTE]
> **Current Project State:** GCP Migration (Phase 2)
> *Status:* Re-provisioning GKE Autopilot to GKE Standard (pd-standard) to resolve SSD quota limits. Migrations pending quota unblock.


## Planning and governance

- `context/plan.md` — canonical phased execution roadmap and reconciliation ledger for pegasusX; every non-trivial implementation batch maps to plan anchors there and updates status when execution reality changes.

## Backend
- **Language**: Go 1.22+
- **Router**: `github.com/go-chi/chi/v5`
- **DB**: Google Cloud Spanner (local: emulator via docker-compose)
- **Cache**: Redis 7 (with Pub/Sub invalidation channel `cache:invalidate`)
- **Events**: Apache Kafka (transactional outbox; sync writer with `RequiredAcks=all`)
- **Logging**: `log/slog` (JSON handler, `trace_id` propagated)
- **Realtime hardening runtime (additive)**: `ws/connection.go` now enforces configurable browser-origin allowlists through `WS_ALLOWED_ORIGINS` while preserving empty-origin native clients and localhost dev origins; `bootstrap/bootstrap.go` now provisions notification-consumer DLQ routing through `KAFKA_TOPIC_MAIN_DLQ` (default `<main-topic>-dlq`) and fails strict-mode startup or disables the consumer with warning when DLQ init fails; `kafka/{consumer.go,dlq_writer.go}` now apply jittered retry backoff, annotate DLQ writes with original topic/partition/offset plus failure reason, and refuse offset commits when DLQ routing fails; `payment/service.go` now verifies raw Adyen signed notification items before business validation or persistence.
- **AI worker monitoring surface (additive)**: `apps/ai-worker/main.go` now exposes `/healthz`, `/ready`, and `/metrics`, emits `void_ai_worker_up`, `void_ai_worker_ready`, and per-topic/partition `void_kafka_consumer_lag_seconds`, derives its bind port from `AI_WORKER_HTTP_PORT` with `HEALTH_PORT` fallback, and lowers readiness before shutdown so orchestration can drain the worker safely.
- **AI recommendation review authority (additive)**: `apps/ai-worker/main.go` now writes explainable `AIPredictions` advisory payloads and emits `AI_RECOMMENDATION_CREATED`; `apps/backend-go/supplier/{ai_recommendations.go,repository_spanner_ai.go}` plus `supplierroutes` expose supplier-scoped `GET|POST /v1/supplier/ai/recommendations`; `schema/spanner.ddl` adds supplier/status recommendation indexes; and supplier decisions are persisted with `decision_history` plus `AI_RECOMMENDATION_DECIDED` outbox emission before `apps/supplier-portal/app/ai/recommendations/page.tsx` renders the review queue through shared contracts.
- **Retailer AI preorder synthesis (additive)**: `apps/ai-worker/main.go` now parses flat `ORDER_CREATED` events, skips non-manual order sources, creates replay-safe derived `AI_PREORDER` order rows keyed by `Orders.DerivedFromOrderId`, emits additive `ORDER_CREATED` preorder events through the same transactional outbox path, and runs a minute-ticker auto-confirm sweep through `order.Service.AutoConfirmDueOrders`. `schema/spanner.ddl` now adds additive order lifecycle columns plus `Idx_Orders_ByConfirmationAutoConfirm` and `Idx_Orders_ByDerivedSource` for bounded auto-confirm and derivation scans.
- **AI worker Kubernetes packaging (additive)**: `infra/k8s/ai-worker/{configmap,deployment,service}.yaml` now package the worker for cluster rollout with config-backed runtime env, a ClusterIP service on `8081`, and liveness/readiness probes against `/healthz` and `/ready`.
- **AI worker manifest gate (additive)**: `scripts/validate_ai_worker_k8s.sh` now parses the worker manifests with Ruby's built-in YAML loader and asserts config map wiring, image placeholder presence, probe paths, and service port contract. Entry points live at `make validate-ai-worker-k8s` and `pnpm infra:k8s:validate`.
- **AI worker observability baseline (additive)**: `infra/terraform/observability.tf` now provisions a launch dashboard, alert policies for `void_ai_worker_up`, `void_ai_worker_ready`, and `void_kafka_consumer_lag_seconds`, plus optional uptime checks against `/healthz` and `/ready` when `enable_observability_resources=true` and `ai_worker_monitoring_host` is set.
- **Launch readiness guard (additive)**: `scripts/validate_launch_readiness.py` now verifies the support runbook bundle, SSMR smoke gate, ai-worker Kubernetes manifest gate, Terraform observability baseline, launch command entrypoints, and context synchronization. Entry points live at `make validate-launch-readiness` and `pnpm infra:launch:validate`, while `docs/LAUNCH_READINESS_RUNBOOK.md` defines the release-owner preflight, rollback, and hypercare procedure.
- **Trace ingress middleware**: `bootstrap.TraceMiddleware` normalizes/generates `X-Trace-Id`, propagates it through request context, and enables additive `trace_id` insertion in outbox payloads for both typed structs and map-based payloads.
- **Sandbox bootstrap command (additive)**: `apps/backend-go/cmd/setup` creates the isolated Spanner instance/database when needed, applies `apps/backend-go/schema/spanner.ddl`, and seeds the single supplier row for local SSMR sandbox bring-up.
- **Smokecheck command (additive)**: `apps/backend-go/cmd/ssmr-smokecheck` asserts seeded supplier + `Retailers` schema state against isolated Spanner, validates Redis-backed perimeter readiness/membership (`spatial` mode), and validates Kafka topic isolation + round-trip flow.
- **Smoke harness (additive)**: `scripts/smoke_ssmr.sh` orchestrates isolated compose bring-up, topic bootstrap, idempotent sandbox bootstrap, Redis ping, backend health, and direct Spanner/spatial/Kafka proof; `make test-ssmr-infra` and `npm run infra:ssmr:test` are thin entrypoints over the same harness.
- **Persistent Go caches (additive)**: `infra/docker-compose.ssmr.yml` shares named `pegasusx-ssmr-go-mod` and `pegasusx-ssmr-go-build` volumes across Go containers, and `scripts/smoke_ssmr.sh` preserves them by tearing the stack down without `-v`.
- **CI stage gate (additive)**: repo-root `.github/workflows/ssmr-infra.yml` runs `make test-ssmr-infra` on `pegasusX/**` push/pull-request changes and manual dispatches.
- **Scaffold outbox runtime**: `bootstrap` now starts `OutboxRelay` with store authority selected at runtime: `outbox/spanner_store.go` (`OutboxEvents` fetch/mark) when Spanner is reachable, with in-memory store fallback when unavailable; repository emit paths use a shared outbox appender seam.
- **Order durability seam (additive)**: `order/repository_spanner.go` now persists `Orders` rows + emitted outbox events in a single Spanner `ReadWriteTransaction`, and `bootstrap` now prefers this repository whenever Spanner runtime wiring succeeds (with in-memory order repository fallback retained).
- **Payment durability seam (additive)**: `payment/repository_spanner.go` now persists payment mutations (`PaymentSessions`, `PaymentAttempts`, `PaymentChargebacks`, `PaymentReversals`, `PaymentWebhooks`) plus emitted outbox events in a single Spanner `ReadWriteTransaction`, and `bootstrap` now prefers this repository whenever Spanner runtime wiring succeeds (with in-memory payment repository fallback retained).
- **Settlement and ledger authority groundwork (additive)**: `schema/spanner.ddl` now provisions immutable `PaymentLedgerEntries`, `payment/repository_spanner.go` now writes ledger entries atomically alongside session/chargeback/reversal/webhook mutation paths and exposes bounded stale-read `ListLedgerEntries`, and `payment/service.go` now serves supplier-scoped `GET /v1/payment/ledger` for finance/support visibility.
- **Settlement and reconciliation authority (additive)**: `payment/service.go` now serves supplier-scoped `GET /v1/payment/settlement/authority` with bounded query filters (`gateway`, `entry_type`, `occurred_from`, `occurred_to`, `group_limit`), `payment/repository_spanner.go` now provides grouped immutable-ledger summaries (`Gateway+EntryType+Currency`) through `SummarizeLedgerEntries`, `paymentroutes/routes.go` mounts this authority endpoint under admin payment scope, and `bootstrap/bootstrap.go` in-memory payment repository preserves summary-query parity.
- **Reconciliation mismatch authority (additive)**: `payment/service.go` now serves supplier-scoped `GET /v1/payment/reconciliation/mismatches` with bounded query filters (`gateway`, `occurred_from`, `occurred_to`, `group_limit`, `mismatch_threshold_minor`), deterministic signed net mismatch aggregation over immutable settlement summary rows, and typed mismatch responses consumed by supplier finance contracts.
- **Supplier finance realtime and treasury operations (additive)**: `supplier/service.go` now exposes `GET /v1/supplier/ws-session` to mint short-lived supplier websocket tokens, `ws/handler.go` accepts signed `?token=` fallback auth for supplier browser sockets, `kafka/notification_dispatcher.go` now fans out supplier-scoped `PAYMENT_REQUIRED`, `PAYMENT_CLEARED`, `SETTLEMENT_REQUIRED`, and `DELIVERY_DISPUTED` envelopes over `supplier:{supplier_id}`, shared `packages/{types,api-client}` contracts now expose typed chargeback/reversal mutations, `apps/supplier-portal/app/payments/page.tsx` now live-refreshes payment authority from that stream, `apps/supplier-portal/app/earnings/page.tsx` is now a real treasury/dispute operations surface, and legacy `GET /v1/supplier/earnings` now reads through a ledger-backed compatibility bridge with explicit `authority_source` and `authoritative` metadata.
- **Warehouse dispatch-lock release hardening (additive)**: `warehouse/service.go` now returns deterministic `404 dispatch_lock_not_found` for `DELETE /v1/warehouse/dispatch-lock` when the target `lock_id` is absent, preventing false release fanout and preserving outbox/cache/ws side effects for real lock transitions only; focused seam coverage now lives in `warehouse/service_test.go` for supply-request and dispatch-lock mutation parity.
- **Factory transfer-cancel idempotency (additive)**: `factory/service.go` now treats repeated `POST /v1/factory/manifests/cancel-transfer` on an already-cancelled transfer as idempotent (`status=already_cancelled`), preventing duplicate exception-count/volume mutation and suppressing extra outbox/cache/ws side effects on replay.
- **Payload reassignment capacity integrity (additive)**: `payload/service.go` now validates target-manifest capacity with reassigned order volume during `POST /v1/payloader/reassign-order` and returns `409 target_manifest_capacity_exceeded` when overflow would occur, preventing order-manifest drift between order row and manifest assignment lists.
- **Factory rebalance no-op and mutability guard (additive)**: `factory/service.go` now treats `POST /v1/factory/manifests/rebalance` requests that keep transfer target unchanged as idempotent (`status=already_assigned`) and rejects rebalance for any non-mutable transfer state (`409 transfer_not_mutable`), preventing no-op/non-mutable reassignment-depth increments and suppressing extra outbox/cache/ws side effects.
- **Payload reassignment target-resolution durability (additive)**: `payload/service.go` now removes fallback-route drift in `POST /v1/payloader/reassign-order` by returning no-op `status=already_assigned` for same-target replay, explicit same-route requests, and mutable-source auto-resolution while preserving deterministic conflicts (`409 reassign_target_unavailable`, `409 target_route_mismatch`) when no mutable/capacity-valid target manifest can be resolved.
- **Factory and payload consistency extension (additive)**: `factory/service.go` now requires rebalance transfer existence in both manifest-local and global transfer ledgers before mutation (`404 transfer_not_found` on global-ledger drift), and `payload/service.go` now allows explicit same-route reassignment to select alternate mutable manifests when the source manifest is non-mutable while preserving mutable-source same-route no-op behavior.
- **Factory and payload cross-entity ownership consistency (additive)**: `factory/service.go` now rejects rebalance with `409 transfer_ledger_mismatch` when manifest-local and global transfer rows diverge on order/state or assignment identity before mutation, and `payload/service.go` now validates source ownership before reassignment mutation (`409 source_manifest_not_found`, `409 source_route_manifest_mismatch`, `409 source_manifest_order_missing`) so source-drift paths cannot mutate state.
- **B05 negative-path fanout envelope contract coverage (additive, test-only)**: `warehouse/service_test.go` now validates `dispatch_lock_not_found` conflict after a successful acquire and asserts no additional outbox/cache/ws fanout envelopes, `factory/service_test.go` now asserts unchanged outbox/ws event-type sequences for `transfer_not_mutable` conflict, rebalance replay `already_assigned`, `transfer_route_mismatch` conflict-after-success, and `already_cancelled` no-op branches, and `payload/service_test.go` now asserts unchanged outbox/ws envelope sequences for replay no-op, `source_manifest_order_missing` conflict-after-success, and `target_route_mismatch` conflict-after-success with explicit websocket envelope data checks.
- **Manifest route geometry + fleet live map (2026-06-11, additive)**: `schema/migrations/20250613_supplier_manifest_route_geometry.ddl` adds `EncodedRoutePolyline` + `RouteGeometrySource` on `SupplierTruckManifests`; `manifest/geometry.go` persists at seal/reorder; `routing/*` builds OSRM or dense polylines; `GET /v1/fleet/route/{routeID}/geometry` serves driver planned overlays; `GET /v1/supplier/fleet/live-map` and `GET /v1/warehouse/ops/fleet/live-map` power operator live maps on supplier-portal, warehouse-portal, supplier-app-ios, and supplier-app-android MapLibre. Driver apps add turn-by-turn and off-route reroute. Ops: `docs/MIGRATION_RUNBOOK_MANIFEST_ROUTE_GEOMETRY.md`. Warehouse native live map remains deferred.
- **B05 closure and B06 kickoff (2026-05-23, additive)**: `context/plan.md` now marks B05 `implemented` and B06 `in progress`; B06 non-technical support artifacts now live in `docs/DRIVER_SUPPORT_PLAYBOOK.md`, `docs/LIVE_TRACKING_EXPECTATIONS.md`, `docs/DELIVERY_ESCALATION_POLICY.md`, and `docs/MIGRATION_RUNBOOK_MANIFEST_ROUTE_GEOMETRY.md` with `docs/README.md` indexing them. PX5-A1 availability, manifest detail, manifest-gate, pending-collections, earnings, and delivery-state durability are landed, PX5-A2 is now implemented with telemetry fanout, durable order assignment/tracking projection, scoped cache-backed retailer live coordinates, supplier-portal live order oversight plus fleet live map, retailer desktop plus retailer iOS plus retailer Android tracking consumers, driver Android plus driver iOS live-state consumers with planned route geometry and breadcrumb overlays, operator fleet live maps on supplier and warehouse portals, and PX5-A3 is now implemented with retailer fulfillment/pending-payment visibility, a derived tracking-event timeline, additive recent completed receipt snapshots, durable `OrderDeliveryProofs`, and an additive receipt dossier carrying immutable payment timelines, recorded gateway webhooks, delivery proofs, chargebacks, and reversals.
- **Retailer fulfillment and pending-payment bridge (additive)**: `retailer/core_handlers.go` now serves `GET /v1/retailer/active-fulfillment` and `GET /v1/retailer/pending-payments` from the existing retailer tracking projection instead of empty scaffold arrays. Both routes reuse live-location enrichment, active fulfillment now returns `{status, fulfillments}` with assignment/location/status metadata, pending payments now returns `{status, count, pending}` filtered to `AWAITING_PAYMENT` and `PENDING_CASH_COLLECTION`, focused coverage lives in `retailer/core_handlers_test.go`, and typed parity now lives in `packages/{types,api-client}`.
- **Retailer preorder lifecycle bridge (additive)**: `order/{service.go,repository_spanner.go}` now own additive retailer preorder lifecycle state on `Orders`, while `retailer/{service.go,core_handlers.go}` delegates `GET /v1/retailer/ai/predictions`, `POST /v1/retailer/orders/{confirm-ai,reject-ai}`, and `POST /v1/orders/{edit-preorder,confirm-preorder}` to that order-owned authority. Typed parity now lives in `packages/{types,api-client}`, and focused coverage lives in `order/service_test.go` plus `retailer/core_handlers_test.go`.
- **Warehouse demand forecast bridge (additive)**: `warehouse/service.go` now resolves `/v1/warehouse/demand/forecast` from the order-owned preorder planner instead of zero-filled buckets, validating node scope plus `start_date`/`days` query input and exposing additive `committed_units` / `pending_confirmation_units` alongside projected units/revenue.
- **Warehouse role-row clients (additive)**: `apps/warehouse-portal` now exposes a direct-backend Next.js/Tauri warehouse dashboard packaged through `src-tauri`, with runtime backend targeting from `NEXT_PUBLIC_WAREHOUSE_BACKEND_BASE_URL` or `NEXT_PUBLIC_BACKEND_BASE_URL`; `apps/warehouse-app-ios` now ships an XcodeGen-backed SwiftUI shell with a direct `URLSession` client and polling view model; and `apps/warehouse-app-android` now ships a Gradle/Compose shell with a direct HTTP client and matching polling dashboard. All three consume dashboard, inventory, orders, demand forecast, supply requests, dispatch preview, and dispatch locks from `warehouseroutes` through shared `packages/{types,api-client}` contracts where applicable. Focused validation passed via successful desktop export, successful `pnpm tauri build --debug`, and iOS `xcodebuild`; all six `*-android` apps ship `gradlew` (PX8-A4).
- **Retailer tracking-event timeline (additive)**: `retailer/core_handlers.go` now serves `GET /v1/retailer/tracking` with derived `events` instead of an unconditional empty array. The handler emits additive `ORDER_CREATED` and `ORDER_STATUS_SNAPSHOT` items from durable order `created_at`, `updated_at`, and `status`, sorts them newest-first, and marks them `derived=true` with `source=ORDER_ROW` so current receipt/payment/completion state is supportable without claiming full outbox-history replay; typed parity now lives in `packages/types`, and focused coverage lives in `retailer/core_handlers_test.go`.
- **Retailer recent receipt visibility (additive)**: `retailer/repository_spanner.go` now reads bounded recent `COMPLETED` `Orders` rows for `GET /v1/retailer/tracking`, `retailer/core_handlers.go` serves them additively as `recent_receipts` while keeping `status` tied to active live-tracking rows, and focused coverage in `retailer/core_handlers_test.go` now proves both active+receipt separation and receipt-only idle snapshots. Shared `packages/types` plus retailer desktop/iOS/Android consumers now render those completed-order snapshots from durable row truth while immutable proof history remains deferred.
- **Retailer receipt dossier (additive)**: `retailer/service.go` now defines `TrackingReceiptDossier`, `TrackingReceiptPaymentRecord`, `TrackingReceiptGatewayWebhook`, `TrackingReceiptDeliveryProof`, `TrackingReceiptChargebackRecord`, `TrackingReceiptReversalRecord`, and `TrackingReceiptProofStatus`, and `retailer/repository_spanner.go` now combines bounded order/session stale reads against immutable `PaymentLedgerEntries`, order-scoped `PaymentWebhooks`, immutable `OrderDeliveryProofs`, order-scoped `PaymentChargebacks`, and session-scoped `PaymentReversals` so `recent_receipts` on `GET /v1/retailer/tracking` carry additive `receipt_dossier` (`payment_timeline`, `gateway_webhooks`, `delivery_proofs`, `chargebacks`, `reversals`, `proof_status`) while preserving backward-compatible `payment_evidence` derived from the dossier head. Shared `packages/types` plus retailer desktop/iOS/Android consumers now render that fuller dispute-support dossier on receipt cards, and `proof_status.delivery_proof_available` now reflects persisted proof artifacts.
- **Order delivery proof durability (additive)**: `schema/spanner.ddl` now includes immutable `OrderDeliveryProofs`, `order/service.go` now derives `QR_HANDOFF`, `FINALIZATION_GEOFENCE`, and `CASH_COLLECTION_GEOFENCE` proof artifacts from real driver transition inputs, and `order/repository_spanner.go` persists those artifacts atomically with the `Orders` row and emitted outbox events so retailer receipt dossiers can read physical proof evidence without mutating order history in place.
- **Driver delivery-state durability (additive)**: `orderroutes/routes.go` now mounts DRIVER-auth compatibility endpoints for `POST /v1/delivery/arrive` and `POST /v1/order/{deliver,confirm-offload,complete,collect-cash}`. `order/service.go` enforces canonical delivery transitions, emits transactional outbox events (`ORDER_STATUS_CHANGED`, `SETTLEMENT_REQUIRED`, `PAYMENT_REQUIRED`, `PAYMENT_CLEARED`, `ORDER_FINALIZED`), invalidates supplier/retailer order caches after commit, fans out supplier/retailer websocket envelopes, and enforces 500m cash geofence validation. `contracts/events.schema.json` and `packages/types` now include `AWAITING_PAYMENT`, `PENDING_CASH_COLLECTION`, `PAYMENT_REQUIRED`, and delivery payment/finalization payload shape parity. Replay against already-target state suppresses extra side effects, and assigned orders require the authenticated DRIVER subject to match `Orders.DriverId`.
- **Order assignment and retailer tracking authority (additive)**: `schema/spanner.ddl` adds nullable `Orders.DriverId`, `VehicleId`, `RouteId`, and `ManifestId` with driver/route/manifest indexes; `orderroutes/routes.go` mounts `POST /v1/orders/{orderID}/assign`; `order/service.go` persists assignment/reassignment through the transactional order repository, emits `ORDER_ASSIGNED`/`ORDER_REASSIGNED`, invalidates supplier/retailer order caches, and fans out supplier/retailer/driver websocket envelopes. `retailer/core_handlers.go` and `retailer/repository_spanner.go` now power `GET /v1/retailer/tracking` from active order rows with assignment metadata; `retailer.HandleTracking` enriches fresh scoped cached driver locations as additive `driver_location` and otherwise keeps `live_location_available=false` truthful.
- **Driver telemetry ingress and live fanout (additive)**: `telemetryroutes/routes.go` now requires authenticated DRIVER claims for `POST /v1/telemetry/location`, derives driver identity from claims, validates coordinate/timestamp input, emits typed `DRIVER_LOCATION_UPDATED` websocket envelopes with `trace_id`, and broadcasts through scoped `TelemetryHub` rooms (`telemetry:driver:{driverID}`, `telemetry:supplier:{supplierID}`). `ws/handler.go` subscribes driver and supplier-side sockets to those rooms, while `ws/connection.go` now guards gorilla writes with a per-connection mutex and ping/pong deadlines for reconnect safety. `telemetry/location_store.go` stores the latest claims-derived driver point in Redis-compatible cache with bounded TTL for retailer tracking enrichment; focused coverage lives in `telemetryroutes/routes_test.go`, `retailer/core_handlers_test.go`, and `ws/handler_test.go`.
- **Supplier order oversight bridge (additive)**: `supplier/repository_spanner.go` now reads recent supplier-scoped `Orders` rows with durable assignment identity, `supplier/portal_handlers.go` enriches those rows with fresh scoped driver locations through the same telemetry cache authority used by retailer tracking while preserving scaffold-only review entries, and `apps/supplier-portal/app/orders/page.tsx` now consumes `GET /v1/supplier/orders` through the same-origin proxy instead of rendering a static placeholder. Shared typing now lives in `packages/{types,api-client}`, giving the supplier portal its first real PX5-A2 consumer while retailer/driver mobile parity stays deferred in pegasusX.
- **Retailer desktop tracking consumer (additive)**: `apps/retailer-app-desktop/app/page.tsx` now consumes `GET /v1/retailer/tracking` through a new same-origin proxy at `apps/retailer-app-desktop/app/api/[...path]/route.ts`, rendering assignment identity, live-vs-stale driver location state, derived order timeline snapshots, and recent completed receipt snapshots plus additive receipt dossiers and backward-compatible latest ledger-backed payment evidence through shared `packages/{types,api-client}` contracts. Focused validation for this tranche passed through clean workspace diagnostics on the touched TS files.
- **Retailer iOS tracking consumer (additive)**: `apps/retailer-app-ios/project.yml` now defines an XcodeGen-backed SwiftUI app target, `Sources/Networking/RetailerAPIClient.swift` now reads `GET /v1/retailer/tracking` directly from backend-go through a safe `URLSession` client with optional bearer-token injection, local contract mirrors now live in `Sources/Models/RetailerTrackingModels.swift`, and `Sources/Views/TrackingDashboardView.swift` renders loading, empty, restricted, error, refreshing, and stale states plus assignment/location/timeline truth and recent completed receipt snapshots plus additive receipt dossiers and backward-compatible latest ledger-backed payment evidence from the existing PX5-A2/PX5-A3 backend projections. Focused validation for this tranche passed through clean workspace diagnostics on the touched Swift files; earlier project-level validation already passed through `xcodegen generate` plus a direct `xcodebuild -project RetailerAppIOS.xcodeproj -target RetailerAppIOS -sdk iphonesimulator26.5 CODE_SIGNING_ALLOWED=NO build`.
- **Retailer Android tracking consumer (additive)**: `apps/retailer-app-android` now carries Gradle Android app metadata (`settings.gradle.kts`, `build.gradle.kts`, `gradle.properties`, `app/build.gradle.kts`), a safe direct tracking client in `app/src/main/kotlin/com/pegasusx/retailerappandroid/network/RetailerTrackingApiClient.kt`, local Kotlin contract mirrors in `app/src/main/kotlin/com/pegasusx/retailerappandroid/model/RetailerTrackingModels.kt`, and a Compose Material 3 dashboard in `app/src/main/kotlin/com/pegasusx/retailerappandroid/ui/TrackingDashboardScreen.kt` that renders loading, empty, restricted, error, refreshing, and stale states plus assignment/location/timeline truth and recent completed receipt snapshots plus additive receipt dossiers and backward-compatible latest ledger-backed payment evidence from the existing PX5-A2/PX5-A3 backend projections. Focused validation for this tranche passed through clean workspace diagnostics on the touched Kotlin files; all role-row Android apps include `gradlew` (PX8-A4).
- **Driver Android live-ops consumer (additive)**: `apps/driver-app-android` now carries Gradle Android app metadata, live-ops client, authenticated `/v1/ws`, Compose dashboard, Google Maps live card, breadcrumb polyline, and planned route geometry from `GET /v1/fleet/route/{routeID}/geometry` with turn-by-turn and off-route reroute. `GET /v1/driver/manifest` remains geometry-free by design.
- **Driver iOS live-ops consumer**: Canonical Xcode project at `apps/driver-app-ios/driverappios` (target `driverappios`) — full SwiftUI execution client with manifest, map, offload, cash, notifications, and authenticated `/v1/ws` on port 8180. The thin PX5-A2 duplicate shell under the outer folder was removed under PX8-A2 (2026-06-04).
- **Driver earnings compatibility bridge (additive)**: `driver/service.go` now serves `GET /v1/driver/earnings` through an optional `EarningsLookup` seam with local scaffold fallback, returns the Pegasus mobile earnings contract (`total_deliveries`, `total_volume`, `total_routes`, `last_30_days`), and adds currency/minor-unit aliases (`currency`, `today_minor`, `week_minor`, `month_minor`, daily `volume_minor`) without adding money mutation or payout math. Because the route is read-only, it intentionally emits no outbox/cache/ws side effects; authoritative completed-delivery sourcing remains deferred to delivery-state durability.
- **Driver pending collections compatibility bridge (additive)**: `driver/service.go` now serves `GET /v1/driver/pending-collections` through an optional `PendingCollectionsLookup` seam with local scaffold fallback, normalizes legacy rows into Pegasus-style `pending_collections` items (`order_id`, `retailer_id`, `amount`, `state`, `updated_at`), and returns `{pending_collections, count}` while preserving legacy `pending` plus `amount_minor`/`due_at` aliases. Because the route is read-only, it intentionally emits no outbox/cache/ws side effects; authoritative order/payment sourcing remains deferred to delivery-state durability.
- **Driver availability mutation durability (additive)**: `driver/service.go` now routes `PATCH /v1/driver/availability` through `repo.Apply`, emits `DRIVER_AVAILABILITY_CHANGED` through `outbox.EmitJSON(events.AggregateDriver, events.TopicMain, ...)`, invalidates `driver:availability:{driverID}` post-commit, and broadcasts committed availability envelopes to both `driver:{driverID}` and `supplier:{supplierID}` websocket rooms. The committed envelope now includes standard realtime metadata (`trace_id`, `timestamp`, `v`, optional `schema_version`) plus additive `available`, preserved `on_shift`, `supplier_id`, and home-node context so mobile websocket consumers can remain contract-accurate while compatibility readers continue to work. Idempotent no-op requests return `200` with `no_change=true` and suppress outbox/cache/ws side effects when the requested on-shift state is already current; `bootstrap/bootstrap.go` now injects cache, hubs, slog, and seeded supplier ID into `driver.NewService`, and focused seam coverage lives in `driver/service_test.go`.
- **Driver manifest gate durability (additive)**: `driver/service.go` now enforces Ghost Stop Prevention on `GET /v1/driver/manifest-gate` by requiring `manifest_id` and resolving gate state from bootstrap-wired factory manifest snapshots; `SEALED`/`DISPATCHED`/`COMPLETED` manifests return `200` with `cleared=true`, compatibility alias `allowed=true`, `stop_count`, and `volume_vu`, unknown manifests return `404 manifest_not_found`, and pre-seal states return `403 AWAITING_PAYLOAD_SEAL`. Because the route is read-only, it intentionally emits no outbox/cache/ws side effects; focused branch coverage now lives in `driver/service_test.go`.
- **Driver manifest detail bridge (additive)**: `driver/service.go` now serves `GET /v1/driver/manifest` and legacy `GET /v1/fleet/manifest` through a bootstrap-wired read-only lookup backed by `factory.Service.ManifestDetailSnapshotForDriver`, replacing the old fake demo hash list with real driver-scoped manifest detail. Responses now project `manifest`, `transfers`, `transitions`, `reassignments`, `exceptions`, `route_id`, `stop_count`, and `order_count` while keeping compatibility explicit with `hashes=[]` and `legacy_hashes_available=false`; focused coverage now lives in `driver/service_test.go` and `factory/service_test.go`.
- **Factory and payload replay-consistency extension (additive)**: factory rebalance now rejects inconsistent transfer linkage (`409 transfer_manifest_mismatch`) and transfer vehicle divergence from manifest-route vehicle (`409 transfer_route_mismatch`) before mutation, payload reassignment now rejects target-driver/manifest divergence (`409 target_driver_manifest_mismatch`), and replaying successful rebalance/reassignment requests now returns deterministic `status=already_assigned` without extra outbox/cache/ws side effects.
- **Supplier durability seam (additive)**: `supplier/repository_spanner.go` now persists supplier onboarding/billing detail in `SupplierProfiles` (seed identity remains in `Suppliers`), and topology read/write is durable through `Warehouses` + `Factories` with transactional outbox buffering.
- **Supplier org-and-fleet onboarding authority (additive)**: `supplier/{onboarding_handlers.go,repository_spanner_onboarding.go}` plus `supplierroutes/routes.go` now expose `GET|POST /v1/supplier/org/members`, `/v1/supplier/fleet/drivers`, and `/v1/supplier/fleet/vehicles`, persist supplier-scoped rows through Spanner with topology validation and optional idempotency replay, and keep supplier login authority narrowed to `SupplierRole='ADMIN'`; `schema/spanner.ddl` now adds `Idx_SupplierUsers_BySupplierUpdated` for index-backed org-member roster reads.
- **Adapter bridge (additive)**: `bootstrap` now attempts Redis transport via `cache/redis_backend.go` and Kafka outbox transport via `outbox/kafka_publisher.go`, with fail-open fallback to in-memory cache/logging publisher when dependencies are unavailable.
- **Strict reliability mode (additive)**: `REQUIRE_INFRA_ADAPTERS=true` enforces fail-fast bootstrap if Redis or Kafka adapter initialization fails; default mode remains fail-open for local/scaffold startup.
- **Request reliability middleware (additive)**: `bootstrap/reliability_middleware.go` applies fixed-window rate limits, priority-based in-flight shedding, and per-class circuit-open responses; mounted in `main.go` immediately after trace middleware and controlled by `RELIABILITY_MIDDLEWARE_ENABLED`.
- **Payment execution router (additive)**: `payment/execution.go` routes provider actions with bounded retries (exponential backoff + jitter), typed policy errors, and gateway capability controls; checkout/chargeback/reversal handlers in `payment/service.go` now invoke this seam before repository persistence/outbox emission.
- **Checkout attempt metadata persistence (additive)**: `payment/service.go` now persists checkout `PaymentSessions` + first `PaymentAttemptRecord` atomically through repository `CreateSessionWithAttempt` and emits additive execution metadata (`attempt_id`, `execution_action`, `execution_mode`, `provider_reference`) in checkout responses and payment-required event payloads.
- **Webhook replay hardening (additive)**: `payment/service.go` now uses non-writing replay checks for Adyen per-item notification loops so duplicate webhook items do not emit concatenated multi-write responses, while maintaining signature-first verification and idempotency safeguards.
- **Webhook handler coverage (additive)**: `payment/service_webhook_handlers_test.go` now covers Global Pay replay dedupe, Stripe replay conflict handling (`409`), Adyen replay single-response behavior, and Adyen signature rejection (`401`).
- **AIRWALLEX direct execution flag**: `AIRWALLEX_DIRECT_EXECUTION_ENABLED` gates AIRWALLEX execution support in the payment router.
- **Strict-mode startup coverage**: `bootstrap/bootstrap_test.go` validates strict-mode fail-fast for missing Redis/Kafka adapters and strict-mode startup success when both adapters are healthy.
- **Outbox Spanner integration coverage**: `outbox/spanner_store_integration_test.go` verifies `Append -> Fetch -> MarkPublished` lifecycle against emulator-backed Spanner when `SPANNER_EMULATOR_HOST` and schema are available (skips gracefully otherwise).
- **WebSocket relay transport**: `ws/hub.go` now relays typed fanout envelopes over `ws:<hub>:fanout` with source-instance suppression; `main.go` starts all role-hub relay subscribers during boot.
- **AuthN**: JWT cookie sessions + optional Firebase ID token verification (RS256 + Google cert rotation cache)
- **Geospatial**: H3 15-char hex on the wire with additive Redis-backed perimeter gating at resolution 9 (`ssmr:delivery_perimeter` + compacted companion set)
- **Core order schema (additive)**: `schema/spanner.ddl` now includes `Orders` with JSON-encoded line items, amount/currency, H3/lat/lng, version, and created/updated timestamps plus supplier/retailer created-at indexes, along with immutable `OrderDeliveryProofs` and `Idx_OrderDeliveryProofs_ByOrderCaptured` for delivery-proof authority.
- **Core payment schema (additive)**: `schema/spanner.ddl` now includes durable payment write tables (`PaymentSessions`, `PaymentAttempts`, `PaymentChargebacks`, `PaymentReversals`, `PaymentWebhooks`, `PaymentLedgerEntries`) with supplier/retailer-scoped payment-session indexes, payment-ledger supplier/order/session indexes, and session/transaction lookup indexes.
- **Spatial perimeter bootstrap (additive)**: `bootstrap/data.go` + `bootstrap/bootstrap.go` precompute and warm supplier delivery coverage in Redis using supplier warehouse coordinates when available (deterministic fallback center/radius otherwise).
- **Topic isolation (additive)**: `events.TopicMain` resolves from `KAFKA_TOPIC_MAIN` at process start, allowing isolated tenant/event-bus naming without changing every outbox emit call site.
- **Order status mutation (additive)**: `POST /v1/order/create` now has a companion `PATCH /v1/order/{orderID}/status` path with transition enforcement, transactional outbox emit (`ORDER_STATUS_CHANGED`), post-commit cache invalidation, and supplier/retailer websocket fanout.
- **Supplier portal API proxy (additive)**: `apps/supplier-portal/app/api/[...path]/route.ts` proxies same-origin `/api/*` calls to backend authority (`SUPPLIER_BACKEND_BASE_URL` / `BACKEND_BASE_URL`) while preserving cookie and idempotency headers for register and billing flows.
- **Supplier portal org-and-fleet surface (additive)**: `apps/supplier-portal/app/org-fleet/page.tsx` now consumes typed `ApiClient` topology, org-member, driver, and vehicle reads plus create flows through the same-origin proxy, and `apps/supplier-portal/app/page.tsx` now links that surface as the supplier-facing PX1-A4 onboarding control.
- **Shared supplier contract bridge (additive)**: `packages/types/index.ts` now exports supplier register/login/configure/billing/profile/topology DTOs, and `packages/api-client/index.ts` now implements typed supplier API methods with `Idempotency-Key` support and structured `ApiError` responses.
- **Supplier payment authority surface (additive)**: `apps/supplier-portal/app/payments/page.tsx` now consumes typed `ApiClient` settlement-authority reads (`getPaymentSettlementAuthority`), mismatch reads (`getPaymentReconciliationMismatches`), and falls back additively to ledger-derived grouping (`getPaymentLedger`) if summary authority is unavailable.
- **Retailer identity hardening (additive)**: `apps/backend-go/retailer/core_handlers.go` now enforces claims-authoritative retailer scope extraction with explicit `401`/`403` mapping and profile `supplier_id` backfill when repository linkage is absent.
- **Order serviceability fail-closed (additive)**: `apps/backend-go/order/{service.go,warehouse_resolver_spanner.go}` now rejects create flows when serviceability authority is unavailable (`delivery_perimeter_unavailable`) or no coverage warehouse exists (`zone_miss`), removing nearest-outside-coverage fallback assignment.
- **Supplier pricing authority (additive)**: `apps/backend-go/supplier/{service.go,portal_handlers.go,repository_spanner.go}` plus `apps/backend-go/supplierroutes/routes.go` now expose `GET|PATCH /v1/supplier/pricing/rules`, backed by durable Spanner `SupplierPricingRules` storage and outbox-based supplier update events.
- **Retailer pricing display bridge (additive)**: `apps/backend-go/retailer/{service.go,core_handlers.go,repository_spanner.go}` now surfaces supplier pricing authority on `GET /v1/retailer/suppliers` as additive `pricing` snapshots and exposes `GET /v1/retailer/pricing/rules` for direct retailer consumption; shared DTO/client coverage lives in `packages/{types,api-client}/index.ts`.
- **Workspace**: `go.work` at `pegasusX/` root listing `./apps/backend-go`, `./apps/ai-worker`, `./packages/config`
- **Route coverage (additive 2026-05-17)**:
	- `supplierroutes`: register/login + supplier core operational surfaces (configure, billing/setup, profile, topology, pricing rules, dashboard, earnings, inventory, inventory-audit, orders, vet, AI recommendations review).
	- `retailerroutes`: register + retailer core operational surfaces (profile, suppliers, pricing rules, cart sync, order cancel/request-cancel, analytics, family-members, AI predictions, AI confirm/reject, preorder, pending-payments, active-fulfillment, tracking).
	- `driverroutes`: driver core operational read/config surfaces (profile, history, earnings, availability, pending-collections, manifest-gate, manifest, legacy fleet manifest alias).
	- `orderroutes`: driver delivery mutation compatibility surfaces (`/v1/delivery/arrive`, `/v1/order/deliver`, `/v1/order/confirm-offload`, `/v1/order/complete`, `/v1/order/collect-cash`) plus retailer/admin order lifecycle surfaces.
	- `warehouseroutes`: warehouse core operational surfaces (ops dashboard, inventory, orders, dispatch preview, order-backed demand forecast, supply requests, dispatch locks).
	- `factoryroutes`: factory core operational surfaces (analytics overview, dashboard, profile, transfers, manifests, manifest detail, manifest lifecycle transitions start-loading/seal/dispatch/complete, rebalance/cancel-transfer/cancel, manifest exception queue, fleet drivers, fleet vehicles, staff, dispatch, supply requests).
	- `payloaderroutes`: payload core operational surfaces (trucks, orders, manifests list/detail, lifecycle transitions start-loading/inject-order/seal, manifest exception write/read, recommend-reassign, reassignment apply, compatibility seal).
	- `paymentroutes`: checkout and payment surfaces (b2b/unified checkout, chargeback, chargeback reversal, supplier-scoped payment ledger read, supplier-scoped settlement-authority read, supplier-scoped reconciliation-mismatches read, deprecated global-pay initiate).
	- `webhookroutes`: gateway callback surfaces (global-pay, adyen, stripe).
	- Retailer protected endpoints are Firebase bearer + `RequireRole(RETAILER)` gated when `FIREBASE_AUTH_ENABLED=true` and verifier wiring is present.
	- Driver protected endpoints are Firebase bearer + `RequireRole(DRIVER)` gated when `FIREBASE_AUTH_ENABLED=true` and verifier wiring is present.
	- Warehouse protected endpoints are Firebase bearer + `RequireRole(WAREHOUSE_ADMIN|ADMIN)` gated when enabled, with cookie `ADMIN` fallback for local scaffold mode.
	- Factory protected endpoints are Firebase bearer + `RequireRole(FACTORY_ADMIN|ADMIN)` gated when enabled, with cookie `ADMIN` fallback for local scaffold mode.
	- Payload protected endpoints are Firebase bearer + `RequireRole(PAYLOAD|ADMIN)` gated when enabled, with cookie `ADMIN` fallback for local scaffold mode.
	- Payment protected endpoints are split by role when enabled: checkout uses Firebase bearer + `RequireRole(RETAILER)`, while payment mutations use Firebase bearer + `RequireRole(ADMIN)` with cookie `ADMIN` fallback for local scaffold mode.
	- Webhook endpoints are JWT-unauthenticated and rely on provider-exact signature verification + transaction-id idempotency (Global Pay Basic auth credentials, Stripe `Stripe-Signature` `t`/`v1`, Adyen item-level `additionalData.hmacSignature`).
	- Factory and payload manifest mutation paths execute through repository `Apply` seams with outbox emit in the same path, followed by cache invalidation and websocket fanout parity.
	- Factory/payload manifest event payload parity now includes additive routing metadata (`route_id`, `driver_id`, `vehicle_id`, `order_count`) on sealed lifecycle events, and payload reassignment envelopes now include `from_manifest_id`/`to_manifest_id` plus route/driver metadata while invalidating both source and target manifest cache keys.

## Route authority freeze (B01)

| Authority surface | Canonical owner | Primary clients | Change rule |
|---|---|---|---|
| `supplierroutes` | supplier platform and business control | supplier portal | changes must update supplier contracts, onboarding/control-tower docs, and supplier role-row parity expectations |
| `retailerroutes` | retailer commerce and account authority | retailer Android, iOS, desktop | changes must update retailer contracts, cart/payment/tracking flows, and customer-support language |
| `driverroutes` | driver execution authority | driver Android, iOS | changes must update driver contracts, manifest semantics, and telemetry/delivery support scripts |
| `warehouseroutes` | warehouse operational authority | warehouse portal, Android, iOS | changes must update warehouse contracts, dispatch/support docs, and node-scoped visibility expectations |
| `factoryroutes` | factory and transfer authority | factory portal, Android, iOS | changes must update manifest contracts, transfer SOPs, and exception handling expectations |
| `payloaderroutes` | loading and manifest repair authority | payload terminal, Android, iOS | changes must update manifest event contracts, exception vocabulary, and source-target cache invalidation rules |
| `orderroutes` | order lifecycle authority | retailer, supplier, downstream operations | changes must preserve additive order states, routing/serviceability authority, and duplicate-request safety |
| `paymentroutes` | checkout and admin payment mutation authority | retailer checkout surfaces, supplier finance/support surfaces | changes must preserve idempotency, policy errors, and replay-safe provider handling |
| `webhookroutes` | gateway callback authority | internal payment and finance flows | changes must keep signature-first verification and transaction-id idempotency intact |
| `telemetryroutes` | live location and execution signal authority | driver apps, supplier and retailer tracking surfaces | changes must keep authenticated identity enforcement and reconnect-safe fanout semantics intact |

## Event catalog freeze (B01)

- Source of truth: `apps/backend-go/events/events.go`, `contracts/events.schema.json`, and shared event unions in `packages/types`.
- Change rule: no event is added, renamed, or repurposed unless these three authorities are updated in the same batch.

Event families:
- Supplier lifecycle: `SUPPLIER_CREATED`, `SUPPLIER_UPDATED`, `SUPPLIER_BILLING_CONFIGURED`
- Retailer and cart: `RETAILER_REGISTERED`, `CART_SYNC_UPDATED`, `SHOP_CLOSED`, `SHOP_CLOSED_RESPONSE`
- Fleet and node operations: `DRIVER_CREATED`, `VEHICLE_CREATED`, `WAREHOUSE_CREATED`, `WAREHOUSE_SUPPLY_REQUEST_OPENED`, `WAREHOUSE_DISPATCH_LOCK_CHANGED`, `FACTORY_CREATED`, `DRIVER_AVAILABILITY_CHANGED`, `DRIVER_LOCATION_UPDATED`
- Order, routing, and AI assistance: `ORDER_CREATED`, `ORDER_STATUS_CHANGED`, `ORDER_VALIDATION_FAILED`, `ORDER_ASSIGNED`, `ORDER_REASSIGNED`, `ORDER_FINALIZED`, `AI_RECOMMENDATION_CREATED`, `AI_RECOMMENDATION_DECIDED`, `ROUTE_CREATED`
- Manifest lifecycle: `MANIFEST_DRAFT_CREATED`, `MANIFEST_LOADING_STARTED`, `MANIFEST_ORDER_INJECTED`, `MANIFEST_ORDER_EXCEPTION`, `MANIFEST_DLQ_ESCALATION`, `MANIFEST_REBALANCED`, `MANIFEST_CANCELLED`, `MANIFEST_SEALED`, `MANIFEST_DISPATCHED`, `MANIFEST_COMPLETED`
- Payment and delivery state: `PAYMENT_REQUIRED`, `PAYMENT_CLEARED`, `SETTLEMENT_REQUIRED`, `DELIVERY_SESSION_UPDATED`, `DELIVERY_DISPUTED`
- Inventory and command/system state: `INVENTORY_SYNC_COMPLETE`, `COMMAND_DISPATCHED`, `COMMAND_RECEIVED`, `COMMAND_SETTLED`, `SYSTEM_APP_OUTDATED`

## Reliability acceptance matrix (B01 draft)

| Area | Required posture | Draft acceptance target | Proof path |
|---|---|---|---|
| Transactional mutations | Spanner RW transaction + outbox emit + post-commit cache invalidation + trace logging | core order and payment write paths complete without duplicate business outcomes under retry and replay | targeted backend tests plus SSMR proof when infra wiring changes |
| Checkout and webhooks | idempotent client retries, provider-exact verification, bounded retry with jitter, typed policy errors | duplicate checkout taps and replayed webhooks collapse to one durable state transition | payment tests, webhook-path validation, provider replay scenarios |
| Kafka event relay | sync-writer semantics for state changes, aggregate-key ordering, replay-safe consumers | state-change event publish lag remains operationally visible and recoverable without silent drop | relay tests, Kafka round-trip SSMR proof, lag dashboards |
| Redis invalidation and websocket fanout | post-commit invalidation, fail-open local broadcast, source-suppressed fanout envelopes | stale reads and duplicate self-echo fanout remain detectable and bounded | cache tests, websocket relay checks, reconnect scenarios |
| Spatial order acceptance | backend-derived H3, warmed perimeter authority, fail-closed `zone_miss` handling | out-of-zone orders are blocked deterministically and in-zone orders stay stable under duplicate attempts | spatial smoke check, order tests, support reproduction steps |
| Driver telemetry and live tracking | authenticated driver identity, reconnect-safe fanout, no trust in client-supplied identity | live tracking recovers after reconnect without corrupting driver identity or route state | telemetry route validation, websocket checks, load-oriented replay drills |
| SSMR local proof gate | isolated Spanner, Redis, Kafka, bootstrap, health, and topic proof on every infra-sensitive change | no infra-sensitive batch is treated as ready without isolated smoke evidence | `scripts/smoke_ssmr.sh`, `make test-ssmr-infra`, CI gate |
| Kubernetes and rollout safety | stateless pods, graceful shutdown, no sticky-session assumptions, explicit rollback path | rolling restart or pod loss does not orphan state-change delivery or force operator re-entry | deployment review, rollout drill, shutdown-path validation |

## Web
- **Framework**: Next.js 15 (App Router) + React 19
- **Styling**: Tailwind v4 + hand-rolled M3 CSS tokens (no `@material/web`)
- **Desktop shell**: Tauri 2 for supplier-portal, retailer-app-desktop, warehouse-portal, factory-portal
- **Charts**: Recharts
- **Maps**: MapLibre GL / Mapbox GL

## Mobile
- **Android**: Kotlin + Jetpack Compose Material 3
- **iOS**: SwiftUI + SF Symbols + system colors
- **Payload Terminal**: Expo / React Native (M3 discipline via RN styling)

## Shared Packages
- `packages/types` — TS canonical contracts
- `packages/api-client` — generated HTTP client
- `packages/validation` — Zod schemas
- `packages/i18n` — copy keys per role
- `packages/ui-kit` — shared desktop foundation + components
- `packages/config` — Go env/boot config

## Contracts
- `contracts/events.schema.json` — canonical websocket/Kafka event schema, generated by backend `cmd/gen-contracts`, consumed by native codegen (Quicktype for Android/iOS).
- Additive event-type coverage includes advanced factory/payload manifest workflow events: `MANIFEST_ORDER_INJECTED`, `MANIFEST_ORDER_EXCEPTION`, `MANIFEST_DLQ_ESCALATION`, `MANIFEST_REBALANCED`, and `MANIFEST_CANCELLED`.
- Additive AI assistance event coverage includes `AI_RECOMMENDATION_CREATED` and `AI_RECOMMENDATION_DECIDED` for supplier review and human-decision audit trails.
- Runtime fanout coverage includes websocket propagation for manifest lifecycle/exception events emitted by factory and payload services: `MANIFEST_LOADING_STARTED`, `MANIFEST_SEALED`, `MANIFEST_ORDER_INJECTED`, `MANIFEST_ORDER_EXCEPTION`, `MANIFEST_DLQ_ESCALATION`, and `MANIFEST_REBALANCED`.

## Infra
- `infra/docker-compose.yml` — local Spanner emulator, Redis, Kafka, Kafka UI
- `infra/docker-compose.ssmr.yml` — isolated SSMR sandbox stack (non-overlapping host ports, Kafka topic bootstrap, backend-go + ai-worker runtime services, ai-worker monitoring exposure on `8181->8081`, Spanner schema/seed bootstrap via `cmd/setup`, explicit `/usr/local/go/bin/go` invocation for Go containers, named Go module/build cache volumes)
- `infra/k8s/ai-worker` — minimal ai-worker cluster packaging (configmap, deployment, ClusterIP service, liveness/readiness probes on the worker monitoring surface)
- `infra/terraform/observability.tf` — optional Cloud Monitoring dashboard / alert / uptime baseline for the ai-worker launch seam
- `scripts/smoke_ssmr.sh` — executable SSMR smoke gate (`make test-ssmr-infra`, `npm run infra:ssmr:test`) proving isolated Spanner/Redis/Kafka/bootstrap wiring, backend health, and Kafka round-trip delivery before teardown
- `../.github/workflows/ssmr-infra.yml` — permanent repo-root pegasusX SSMR stage-gate running `make test-ssmr-infra` on `pegasusX/**` changes and manual dispatches
- `infra/terraform/` — baseline cloud provisioning (VPC, Spanner, Redis, Secret Manager for Kafka/Firebase runtime wiring) with `tenant_slug` / `resource_prefix` segregation and distinct orders/spatial/realtime/webhook topic secret outputs
- `infra/k8s/` — deployment manifests
