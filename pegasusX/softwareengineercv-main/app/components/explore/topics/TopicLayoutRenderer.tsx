'use client';

/**
 * Legacy renderer — topics now use O9DetailLayout via TopicPageClient.
 * Kept as a thin re-export so old imports do not break.
 */
export { default } from '@/app/components/explore/O9DetailLayout';
