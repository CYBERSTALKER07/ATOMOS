diff --git a/pegasusX/apps/admin-portal/README.md b/pegasusX/apps/admin-portal/README.md
index 54642e296..79bbf9b5a 100644
--- a/pegasusX/apps/admin-portal/README.md
+++ b/pegasusX/apps/admin-portal/README.md
@@ -1,16 +1,23 @@
 # Deprecated — use `supplier-portal`
 
-This directory is a retired stub from the Pegasus → pegasusX rename.
+This directory is a **retired discoverability stub** from the Pegasus → pegasusX rename.
 
 **Canonical supplier / admin surface:** [`../supplier-portal`](../supplier-portal)
 
-- Web + Tauri desktop: `pegasusX/apps/supplier-portal`
-- Native: `supplier-app-ios`, `supplier-app-android`
-- Backend APIs: `/v1/supplier/*` on `backend-go`
+| Capability | Location |
+|------------|----------|
+| Web + Tauri desktop | `pegasusX/apps/supplier-portal` |
+| Native mobile | `supplier-app-ios`, `supplier-app-android` |
+| Backend APIs | `/v1/supplier/*`, admin payment/order routes on `backend-go` |
 
-**Admin order operations** (assign driver, patch status) live on the supplier portal **Orders** page when signed in with an eligible JWT:
+## Admin order operations
 
-- **Assign** (`POST /v1/orders/{orderID}/assign`): `ADMIN`, `WAREHOUSE_ADMIN`, `FACTORY_ADMIN` — key `adminOrderAssignKey`
-- **Status patch** (`PATCH /v1/order/{orderID}/status`): `ADMIN` only on this surface — key `adminOrderStatusPatchKey`
+Live on the supplier portal **Orders** page when signed in with an eligible JWT:
 
-Do not add features here. Reference-only Pegasus UI measurements live under `pegasus/apps/admin-portal` (read-only).
+| Action | Method / path | Roles |
+|--------|---------------|-------|
+| Assign driver | `POST /v1/orders/{orderID}/assign` | `ADMIN`, `WAREHOUSE_ADMIN`, `FACTORY_ADMIN` |
+| Status patch | `PATCH /v1/order/{orderID}/status` | `ADMIN` |
+| Chargeback / ledger | `/v1/payment/*` | `ADMIN` |
+
+Do **not** add product features here. Running `pnpm dev` / `pnpm build` in this folder prints the redirect and exits non-zero (build check exits 0).
diff --git a/pegasusX/apps/admin-portal/package.json b/pegasusX/apps/admin-portal/package.json
new file mode 100644
index 000000000..8050e0362
--- /dev/null
+++ b/pegasusX/apps/admin-portal/package.json
@@ -0,0 +1,13 @@
+{
+  "name": "@pegasusx/admin-portal",
+  "private": true,
+  "version": "0.0.0",
+  "description": "Deprecated stub — canonical admin/supplier surface is supplier-portal",
+  "scripts": {
+    "dev": "node ./redirect.mjs",
+    "build": "node ./redirect.mjs --check",
+    "start": "node ./redirect.mjs",
+    "tauri:dev": "node ./redirect.mjs",
+    "tauri:build": "node ./redirect.mjs --check"
+  }
+}
diff --git a/pegasusX/apps/admin-portal/redirect.mjs b/pegasusX/apps/admin-portal/redirect.mjs
new file mode 100644
index 000000000..827daaab0
--- /dev/null
+++ b/pegasusX/apps/admin-portal/redirect.mjs
@@ -0,0 +1,21 @@
+#!/usr/bin/env node
+/**
+ * Enterprise discoverability stub for retired admin-portal.
+ * Canonical surface: apps/supplier-portal (web + Tauri).
+ */
+const checkOnly = process.argv.includes("--check");
+const target = new URL("../supplier-portal", import.meta.url).pathname;
+
+const message = [
+  "",
+  "admin-portal is retired.",
+  "Use supplier-portal for supplier + admin order/finance ops:",
+  `  cd ${target}`,
+  "  pnpm run tauri:dev   # or pnpm dev",
+  "",
+  "Admin-capable routes require ADMIN (or WAREHOUSE_ADMIN/FACTORY_ADMIN) JWT.",
+  "",
+].join("\n");
+
+console.error(message);
+process.exit(checkOnly ? 0 : 1);
diff --git a/pegasusX/apps/factory-portal/components/ClientPolicyBanner.tsx b/pegasusX/apps/factory-portal/components/ClientPolicyBanner.tsx
index d32f4a916..b9b4b5a19 100644
--- a/pegasusX/apps/factory-portal/components/ClientPolicyBanner.tsx
+++ b/pegasusX/apps/factory-portal/components/ClientPolicyBanner.tsx
@@ -2,6 +2,7 @@
 
 import { useEffect, useState } from "react";
 import { AlertTriangle } from "lucide-react";
+import { desktopClientPolicyContext } from "@pegasusx/desktop-bridge";
 import { getClientPolicy } from "@/lib/client-policy";
 
 type PolicyState = {
@@ -17,8 +18,10 @@ export default function ClientPolicyBanner() {
 
   useEffect(() => {
     const version =
-      process.env.NEXT_PUBLIC_FACTORY_APP_VERSION?.trim() || "1.0.0";
-    void getClientPolicy("web", version)
+      process.env.NEXT_PUBLIC_FACTORY_APP_VERSION?.trim() || "0.1.0";
+    // Tauri: enterprise → website CDN; store → production (MS/Mac App Store).
+    const { platform, channel } = desktopClientPolicyContext();
+    void getClientPolicy(platform, version, channel)
       .then(async (res) => {
         if (!res.ok) return;
         const data = (await res.json()) as {
diff --git a/pegasusX/apps/factory-portal/components/Providers.tsx b/pegasusX/apps/factory-portal/components/Providers.tsx
index f95360e19..095c067ac 100644
--- a/pegasusX/apps/factory-portal/components/Providers.tsx
+++ b/pegasusX/apps/factory-portal/components/Providers.tsx
@@ -9,11 +9,13 @@ import { ToastProvider } from "./Toast";
 import { ThemeProvider } from "./ThemeProvider";
 import { PortalOfflineTray } from "@/lib/portal-offline-tray";
 import { DesktopDeepLinkBootstrap } from "@/lib/desktop-deep-link";
+import { EnterpriseDesktopUpdateBootstrap } from "@/lib/desktop-updater";
 
 export default function Providers({ children }: { children: ReactNode }) {
   return (
     <ThemeProvider>
       <DesktopDeepLinkBootstrap />
+      <EnterpriseDesktopUpdateBootstrap />
       <div className="app-root min-h-screen w-full">
         <AuthGuard>
           <FactoryShell>
diff --git a/pegasusX/apps/factory-portal/package.json b/pegasusX/apps/factory-portal/package.json
index 09720f8b5..5e292fc7e 100644
--- a/pegasusX/apps/factory-portal/package.json
+++ b/pegasusX/apps/factory-portal/package.json
@@ -16,7 +16,12 @@
     "tauri:build": "TAURI_BUILD=1 tauri build",
     "tauri:build:mac": "TAURI_BUILD=1 tauri build --target universal-apple-darwin",
     "tauri:build:win": "TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc",
-    "tauri:build:debug": "TAURI_BUILD=1 tauri build --debug"
+    "tauri:build:debug": "TAURI_BUILD=1 tauri build --debug",
+    "tauri:build:store": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --config src-tauri/tauri.store.conf.json",
+    "tauri:build:store:win": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc --config src-tauri/tauri.microsoftstore.conf.json",
+    "tauri:build:store:mac": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --target universal-apple-darwin --config src-tauri/tauri.macappstore.conf.json",
+    "tauri:build:enterprise:win": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=enterprise TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc",
+    "tauri:build:enterprise:mac": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=enterprise TAURI_BUILD=1 tauri build --target universal-apple-darwin"
   },
   "dependencies": {
     "@heroui/react": "^3.1.0",
@@ -33,6 +38,8 @@
     "@tauri-apps/plugin-dialog": "^2.7.0",
     "@tauri-apps/plugin-fs": "^2.5.0",
     "@tauri-apps/plugin-deep-link": "^2.4.9",
+    "@tauri-apps/plugin-process": "^2.3.1",
+    "@tauri-apps/plugin-updater": "^2.9.0",
     "firebase": "^12.12.0",
     "framer-motion": "^12.38.0",
     "lucide-react": "^1.7.0",
diff --git a/pegasusX/apps/factory-portal/src-tauri/Cargo.toml b/pegasusX/apps/factory-portal/src-tauri/Cargo.toml
index f6429fda8..0bb93282f 100644
--- a/pegasusX/apps/factory-portal/src-tauri/Cargo.toml
+++ b/pegasusX/apps/factory-portal/src-tauri/Cargo.toml
@@ -28,6 +28,7 @@ env_logger = "0.11"
 [target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
 tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }
 tauri-plugin-deep-link = "2"
+tauri-plugin-updater = "2"
 
 [features]
 default = ["custom-protocol"]
diff --git a/pegasusX/apps/factory-portal/src-tauri/capabilities/default.json b/pegasusX/apps/factory-portal/src-tauri/capabilities/default.json
index 9421cf4c3..c7ba1b433 100644
--- a/pegasusX/apps/factory-portal/src-tauri/capabilities/default.json
+++ b/pegasusX/apps/factory-portal/src-tauri/capabilities/default.json
@@ -25,6 +25,7 @@
     "os:default",
     "process:default",
     "process:allow-exit",
-    "process:allow-restart"
+    "process:allow-restart",
+    "updater:default"
   ]
 }
diff --git a/pegasusX/apps/factory-portal/src-tauri/src/lib.rs b/pegasusX/apps/factory-portal/src-tauri/src/lib.rs
index d16b0931b..9cc91e347 100644
--- a/pegasusX/apps/factory-portal/src-tauri/src/lib.rs
+++ b/pegasusX/apps/factory-portal/src-tauri/src/lib.rs
@@ -22,7 +22,8 @@ pub fn run() {
                     let _ = window.set_focus();
                 }
             }))
-            .plugin(tauri_plugin_deep_link::init());
+            .plugin(tauri_plugin_deep_link::init())
+            .plugin(tauri_plugin_updater::Builder::new().build());
     }
 
     builder
diff --git a/pegasusX/apps/factory-portal/src-tauri/tauri.conf.json b/pegasusX/apps/factory-portal/src-tauri/tauri.conf.json
index bceb23540..184c758ef 100644
--- a/pegasusX/apps/factory-portal/src-tauri/tauri.conf.json
+++ b/pegasusX/apps/factory-portal/src-tauri/tauri.conf.json
@@ -31,19 +31,12 @@
     "security": {
       "dangerousDisableAssetCspModification": false,
       "csp": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: blob:; connect-src 'self' http://localhost:8180 http://127.0.0.1:8180 http://localhost:9099 http://127.0.0.1:9099 https://factory.void.pegasus.uz wss://localhost:8180 ws://localhost:8180"
-    },
-    "updater": {
-      "active": true,
-      "endpoints": [
-        "https://storage.googleapis.com/pegasusx-ssmr-app-updates/factory-desktop/{{target}}/{{arch}}/updater.json"
-      ],
-      "dialog": true,
-      "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDhDREMyMEEzRkY5NEYzQjMKUldTejg1VC9veURjakZ6R3NoSmZ6NGIyOGVBZVBZZGs5TkdMWk9XRTArQ2UyakJNZ01XYXZtS0UK"
     }
   },
   "bundle": {
     "active": true,
     "targets": "all",
+    "createUpdaterArtifacts": true,
     "icon": [
       "icons/32x32.png",
       "icons/128x128.png",
@@ -64,6 +57,15 @@
       "desktop": {
         "schemes": ["pegasusx-factory"]
       }
+    },
+    "updater": {
+      "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDhDREMyMEEzRkY5NEYzQjMKUldTejg1VC9veURjakZ6R3NoSmZ6NGIyOGVBZVBZZGs5TkdMWk9XRTArQ2UyakJNZ01XYXZtS0UK",
+      "endpoints": [
+        "https://storage.googleapis.com/pegasusx-ssmr-app-updates/factory-desktop/{{target}}/{{arch}}/updater.json"
+      ],
+      "windows": {
+        "installMode": "passive"
+      }
     }
   }
 }
diff --git a/pegasusX/apps/factory-portal/src-tauri/tauri.macappstore.conf.json b/pegasusX/apps/factory-portal/src-tauri/tauri.macappstore.conf.json
new file mode 100644
index 000000000..78f357c8f
--- /dev/null
+++ b/pegasusX/apps/factory-portal/src-tauri/tauri.macappstore.conf.json
@@ -0,0 +1,18 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX",
+    "targets": [
+      "app",
+      "dmg"
+    ],
+    "macOS": {
+      "minimumSystemVersion": "12.0"
+    }
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
diff --git a/pegasusX/apps/factory-portal/src-tauri/tauri.microsoftstore.conf.json b/pegasusX/apps/factory-portal/src-tauri/tauri.microsoftstore.conf.json
new file mode 100644
index 000000000..6ef2926a1
--- /dev/null
+++ b/pegasusX/apps/factory-portal/src-tauri/tauri.microsoftstore.conf.json
@@ -0,0 +1,16 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX",
+    "windows": {
+      "webviewInstallMode": {
+        "type": "offlineInstaller"
+      }
+    }
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
diff --git a/pegasusX/apps/factory-portal/src-tauri/tauri.store.conf.json b/pegasusX/apps/factory-portal/src-tauri/tauri.store.conf.json
new file mode 100644
index 000000000..9af34067b
--- /dev/null
+++ b/pegasusX/apps/factory-portal/src-tauri/tauri.store.conf.json
@@ -0,0 +1,11 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX"
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
diff --git a/pegasusX/apps/supplier-portal/app/payments/_shared/finance.ts b/pegasusX/apps/supplier-portal/app/payments/_shared/finance.ts
index 3d7c30e14..6da0845f8 100644
--- a/pegasusX/apps/supplier-portal/app/payments/_shared/finance.ts
+++ b/pegasusX/apps/supplier-portal/app/payments/_shared/finance.ts
@@ -38,6 +38,10 @@ interface SupplierWebSocketSessionResponse {
 const supplierFinanceRefreshEventTypes = new Set([
   "PAYMENT_REQUIRED",
   "PAYMENT_CLEARED",
+  "FISCAL_RECEIPT_REQUESTED",
+  "FISCAL_RECEIPT_SUCCEEDED",
+  "FISCAL_RECEIPT_FAILED",
+  "ORDER_FORCE_COMPLETED",
   "SETTLEMENT_REQUIRED",
   "DELIVERY_DISPUTED",
 ]);
diff --git a/pegasusX/apps/supplier-portal/components/AdminOrderOpsPanel.tsx b/pegasusX/apps/supplier-portal/components/AdminOrderOpsPanel.tsx
index 4db98563e..315b6a655 100644
--- a/pegasusX/apps/supplier-portal/components/AdminOrderOpsPanel.tsx
+++ b/pegasusX/apps/supplier-portal/components/AdminOrderOpsPanel.tsx
@@ -2,12 +2,14 @@
 
 import { useState } from 'react';
 import {
+  adminForceCompleteKey,
   adminOrderAssignKey,
   adminOrderStatusPatchKey,
   ApiError,
 } from '@pegasusx/api-client';
 import type { SupplierOrder } from '@pegasusx/types';
 import { createSupplierApi } from '@/lib/api';
+import StatusChip from '@/components/StatusChip';
 
 const STATUS_OPTIONS = [
   'PENDING',
@@ -19,6 +21,14 @@ const STATUS_OPTIONS = [
   'COMPLETED',
 ] as const;
 
+const FORCE_REASON_OPTIONS = [
+  'OFD_DOWN',
+  'OFD_TIMEOUT',
+  'OPS_ESCALATION',
+  'TAX_EXEMPT_POLICY',
+  'OTHER',
+] as const;
+
 const supplierApi = createSupplierApi();
 
 interface AdminOrderOpsPanelProps {
@@ -46,6 +56,20 @@ export function AdminOrderOpsPanel({
   const [vehicleId, setVehicleId] = useState(order.vehicle_id ?? '');
   const [nextStatus, setNextStatus] = useState(order.status);
   const [statusReason, setStatusReason] = useState('');
+  const [forceReason, setForceReason] = useState<string>('OFD_DOWN');
+
+  const statusUpper = (order.status || '').toUpperCase();
+  const canForceComplete =
+    statusUpper === 'FISCAL_FAILED' || statusUpper === 'FISCALIZING';
+  const fiscalLabel = order.fiscal_status
+    ? String(order.fiscal_status)
+    : statusUpper === 'FISCALIZING'
+      ? 'PENDING'
+      : statusUpper === 'FISCAL_FAILED'
+        ? 'FAILED'
+        : statusUpper === 'COMPLETED'
+          ? 'SUCCESS'
+          : undefined;
 
   const assignDriver = async () => {
     const trimmedDriver = driverId.trim();
@@ -94,8 +118,38 @@ export function AdminOrderOpsPanel({
     }
   };
 
+  const forceComplete = async () => {
+    const reason = forceReason.trim();
+    if (!reason) {
+      onError('Reason code is required for force-complete.');
+      return;
+    }
+    onBusyChange(order.order_id);
+    try {
+      await supplierApi.forceCompleteOrder(
+        order.order_id,
+        { reason_code: reason },
+        adminForceCompleteKey(order.order_id, reason),
+      );
+      onSuccess();
+    } catch (err) {
+      onError(err instanceof ApiError ? err.message : 'force_complete_failed');
+    } finally {
+      onBusyChange(null);
+    }
+  };
+
   return (
     <div className="mt-2">
+      {fiscalLabel ? (
+        <div className="mb-2 flex flex-wrap items-center gap-2">
+          <span className="md-typescale-label-small text-[var(--color-md-on-surface-variant)]">
+            Fiscal
+          </span>
+          <StatusChip status={fiscalLabel} label={`Fiscal ${fiscalLabel}`} size="sm" />
+          <StatusChip status={order.status} size="sm" />
+        </div>
+      ) : null}
       <button
         type="button"
         className="md-btn md-btn-text md-typescale-label-medium px-0"
@@ -105,6 +159,36 @@ export function AdminOrderOpsPanel({
       </button>
       {open ? (
         <div className="mt-3 space-y-4 rounded-2xl border border-[var(--color-md-outline-variant)] bg-[var(--color-md-surface-container-low)] p-4">
+          {canForceComplete ? (
+            <div>
+              <p className="md-typescale-label-medium mb-2">Force-complete (fiscal exception)</p>
+              <p className="md-typescale-body-small mb-2 text-[var(--color-md-on-surface-variant)]">
+                Audited skip of OFD when fiscal is stuck. Requires reason code. Not for drivers.
+              </p>
+              <div className="flex flex-col gap-2 sm:flex-row">
+                <select
+                  className="md-input-outlined flex-1 px-3 py-2"
+                  value={forceReason}
+                  onChange={(e) => setForceReason(e.target.value)}
+                  disabled={busy}
+                >
+                  {FORCE_REASON_OPTIONS.map((code) => (
+                    <option key={code} value={code}>
+                      {code}
+                    </option>
+                  ))}
+                </select>
+                <button
+                  type="button"
+                  className="md-btn md-btn-filled"
+                  onClick={forceComplete}
+                  disabled={busy}
+                >
+                  Force complete
+                </button>
+              </div>
+            </div>
+          ) : null}
           {canAssign ? (
           <div>
             <p className="md-typescale-label-medium mb-2">Assign driver</p>
diff --git a/pegasusX/apps/supplier-portal/components/ClientPolicyBanner.tsx b/pegasusX/apps/supplier-portal/components/ClientPolicyBanner.tsx
index 47354e215..de69b00aa 100644
--- a/pegasusX/apps/supplier-portal/components/ClientPolicyBanner.tsx
+++ b/pegasusX/apps/supplier-portal/components/ClientPolicyBanner.tsx
@@ -2,6 +2,7 @@
 
 import { useEffect, useState } from "react";
 import { AlertTriangle } from "lucide-react";
+import { desktopClientPolicyContext } from "@pegasusx/desktop-bridge";
 import { getClientPolicy } from "@/lib/client-policy";
 
 type PolicyState = {
@@ -17,8 +18,10 @@ export default function ClientPolicyBanner() {
 
   useEffect(() => {
     const version =
-      process.env.NEXT_PUBLIC_SUPPLIER_APP_VERSION?.trim() || "1.0.0";
-    void getClientPolicy("web", version)
+      process.env.NEXT_PUBLIC_SUPPLIER_APP_VERSION?.trim() || "0.1.0";
+    // Tauri: enterprise → website CDN; store → production (MS/Mac App Store).
+    const { platform, channel } = desktopClientPolicyContext();
+    void getClientPolicy(platform, version, channel)
       .then(async (res) => {
         if (!res.ok) return;
         const data = (await res.json()) as {
diff --git a/pegasusX/apps/supplier-portal/components/Providers.tsx b/pegasusX/apps/supplier-portal/components/Providers.tsx
index 304ab178e..512deaf03 100644
--- a/pegasusX/apps/supplier-portal/components/Providers.tsx
+++ b/pegasusX/apps/supplier-portal/components/Providers.tsx
@@ -5,6 +5,7 @@ import SupplierShell from "./SupplierShell";
 import { ThemeProvider } from "./ThemeProvider";
 import { DesktopCacheBootstrap } from "@/lib/desktop-cache-bootstrap";
 import { DesktopDeepLinkBootstrap } from "@/lib/desktop-deep-link";
+import { EnterpriseDesktopUpdateBootstrap } from "@/lib/desktop-updater";
 import { PortalOfflineTray } from "@/lib/portal-offline-tray";
 
 export default function Providers({ children }: { children: ReactNode }) {
@@ -12,6 +13,7 @@ export default function Providers({ children }: { children: ReactNode }) {
     <ThemeProvider>
       <DesktopCacheBootstrap />
       <DesktopDeepLinkBootstrap />
+      <EnterpriseDesktopUpdateBootstrap />
       <div className="app-root min-h-screen w-full">
         <SupplierShell>{children}</SupplierShell>
         <PortalOfflineTray />
diff --git a/pegasusX/apps/supplier-portal/components/StatusChip.tsx b/pegasusX/apps/supplier-portal/components/StatusChip.tsx
index 581509464..770639c06 100644
--- a/pegasusX/apps/supplier-portal/components/StatusChip.tsx
+++ b/pegasusX/apps/supplier-portal/components/StatusChip.tsx
@@ -10,6 +10,8 @@ const STATUS_MAP: Record<string, { tone: string }> = {
   ARRIVED_SHOP_CLOSED:     { tone: 'warning-strong' },
   AWAITING_PAYMENT:        { tone: 'warning' },
   PENDING_CASH_COLLECTION: { tone: 'warning' },
+  FISCALIZING:             { tone: 'warning' },
+  FISCAL_FAILED:           { tone: 'danger' },
   COMPLETED:               { tone: 'success-strong' },
   CANCELLED:               { tone: 'danger' },
   CANCEL_REQUESTED:        { tone: 'accent' },
diff --git a/pegasusX/apps/supplier-portal/lib/order-actions.ts b/pegasusX/apps/supplier-portal/lib/order-actions.ts
index 36b205a47..307ec8f80 100644
--- a/pegasusX/apps/supplier-portal/lib/order-actions.ts
+++ b/pegasusX/apps/supplier-portal/lib/order-actions.ts
@@ -33,6 +33,8 @@ export const ORDER_STATE_CHIP: Record<string, string> = {
   DELAYED: 'status-chip--draft',
   ARRIVED: 'status-chip--ready',
   COMPLETED: 'status-chip--stable',
+  FISCALIZING: 'status-chip--warning',
+  FISCAL_FAILED: 'status-chip--danger',
   CANCELLED: 'status-chip--critical',
   SCHEDULED: 'status-chip--draft',
   AUTO_ACCEPTED: 'status-chip--ready',
diff --git a/pegasusX/apps/supplier-portal/package.json b/pegasusX/apps/supplier-portal/package.json
index dea116003..f3966cbbd 100644
--- a/pegasusX/apps/supplier-portal/package.json
+++ b/pegasusX/apps/supplier-portal/package.json
@@ -17,7 +17,12 @@
     "tauri:build:mac": "TAURI_BUILD=1 tauri build --target universal-apple-darwin",
     "tauri:build:win": "TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc",
     "tauri:android:dev": "tauri android dev",
-    "tauri:android:build": "TAURI_BUILD=1 tauri android build"
+    "tauri:android:build": "TAURI_BUILD=1 tauri android build",
+    "tauri:build:store": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --config src-tauri/tauri.store.conf.json",
+    "tauri:build:store:win": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc --config src-tauri/tauri.microsoftstore.conf.json",
+    "tauri:build:store:mac": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --target universal-apple-darwin --config src-tauri/tauri.macappstore.conf.json",
+    "tauri:build:enterprise:win": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=enterprise TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc",
+    "tauri:build:enterprise:mac": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=enterprise TAURI_BUILD=1 tauri build --target universal-apple-darwin"
   },
   "dependencies": {
     "@heroui/react": "^3.0.2",
@@ -34,7 +39,9 @@
     "@tauri-apps/plugin-dialog": "^2.7.0",
     "@tauri-apps/plugin-fs": "^2.5.0",
     "@tauri-apps/plugin-deep-link": "^2.4.9",
+    "@tauri-apps/plugin-process": "^2.3.1",
     "@tauri-apps/plugin-sql": "^2.2.0",
+    "@tauri-apps/plugin-updater": "^2.9.0",
     "firebase": "^12.12.0",
     "framer-motion": "^12.42.2",
     "h3-js": "^4.5.0",
diff --git a/pegasusX/apps/supplier-portal/src-tauri/Cargo.toml b/pegasusX/apps/supplier-portal/src-tauri/Cargo.toml
index a0e5b92cc..80a8f6d96 100644
--- a/pegasusX/apps/supplier-portal/src-tauri/Cargo.toml
+++ b/pegasusX/apps/supplier-portal/src-tauri/Cargo.toml
@@ -21,12 +21,14 @@ tauri = { version = "2", features = ["macos-private-api"] }
 tauri-plugin-shell = "2"
 tauri-plugin-dialog = "2"
 tauri-plugin-fs = "2"
+tauri-plugin-process = "2"
 tauri-plugin-sql = { version = "2", features = ["sqlite"] }
 keyring = { version = "3", features = ["apple-native", "windows-native"] }
 
 [target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
 tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }
 tauri-plugin-deep-link = "2"
+tauri-plugin-updater = "2"
 
 [features]
 default = ["custom-protocol"]
diff --git a/pegasusX/apps/supplier-portal/src-tauri/capabilities/default.json b/pegasusX/apps/supplier-portal/src-tauri/capabilities/default.json
index cc1c0523d..29b496e8d 100644
--- a/pegasusX/apps/supplier-portal/src-tauri/capabilities/default.json
+++ b/pegasusX/apps/supplier-portal/src-tauri/capabilities/default.json
@@ -9,6 +9,9 @@
     "dialog:default",
     "dialog:allow-save",
     "fs:default",
-    "fs:allow-write-text-file"
+    "fs:allow-write-text-file",
+    "process:default",
+    "process:allow-restart",
+    "updater:default"
   ]
 }
diff --git a/pegasusX/apps/supplier-portal/src-tauri/src/lib.rs b/pegasusX/apps/supplier-portal/src-tauri/src/lib.rs
index dca79f851..3523ef40b 100644
--- a/pegasusX/apps/supplier-portal/src-tauri/src/lib.rs
+++ b/pegasusX/apps/supplier-portal/src-tauri/src/lib.rs
@@ -21,7 +21,9 @@ pub fn run() {
                     let _ = window.set_focus();
                 }
             }))
-            .plugin(tauri_plugin_deep_link::init());
+            .plugin(tauri_plugin_deep_link::init())
+            .plugin(tauri_plugin_updater::Builder::new().build())
+            .plugin(tauri_plugin_process::init());
     }
 
     builder
diff --git a/pegasusX/apps/supplier-portal/src-tauri/tauri.conf.json b/pegasusX/apps/supplier-portal/src-tauri/tauri.conf.json
index 664e1dfbc..64a8b72dd 100644
--- a/pegasusX/apps/supplier-portal/src-tauri/tauri.conf.json
+++ b/pegasusX/apps/supplier-portal/src-tauri/tauri.conf.json
@@ -30,19 +30,12 @@
     "security": {
       "csp": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob:; connect-src 'self' http://localhost:8180 http://127.0.0.1:8180 http://localhost:9099 http://127.0.0.1:9099 http://10.0.2.2:8180 ws://localhost:8180 ws://127.0.0.1:8180 ws://10.0.2.2:8180 wss://localhost:8180"
     },
-    "macOSPrivateApi": true,
-    "updater": {
-      "active": true,
-      "endpoints": [
-        "https://storage.googleapis.com/pegasusx-ssmr-app-updates/supplier-desktop/{{target}}/{{arch}}/updater.json"
-      ],
-      "dialog": true,
-      "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDhDREMyMEEzRkY5NEYzQjMKUldTejg1VC9veURjakZ6R3NoSmZ6NGIyOGVBZVBZZGs5TkdMWk9XRTArQ2UyakJNZ01XYXZtS0UK"
-    }
+    "macOSPrivateApi": true
   },
   "bundle": {
     "active": true,
     "targets": "all",
+    "createUpdaterArtifacts": true,
     "icon": [
       "icons/32x32.png",
       "icons/128x128.png",
@@ -63,6 +56,15 @@
       "desktop": {
         "schemes": ["pegasusx-supplier"]
       }
+    },
+    "updater": {
+      "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDhDREMyMEEzRkY5NEYzQjMKUldTejg1VC9veURjakZ6R3NoSmZ6NGIyOGVBZVBZZGs5TkdMWk9XRTArQ2UyakJNZ01XYXZtS0UK",
+      "endpoints": [
+        "https://storage.googleapis.com/pegasusx-ssmr-app-updates/supplier-desktop/{{target}}/{{arch}}/updater.json"
+      ],
+      "windows": {
+        "installMode": "passive"
+      }
     }
   }
 }
diff --git a/pegasusX/apps/supplier-portal/src-tauri/tauri.macappstore.conf.json b/pegasusX/apps/supplier-portal/src-tauri/tauri.macappstore.conf.json
new file mode 100644
index 000000000..78f357c8f
--- /dev/null
+++ b/pegasusX/apps/supplier-portal/src-tauri/tauri.macappstore.conf.json
@@ -0,0 +1,18 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX",
+    "targets": [
+      "app",
+      "dmg"
+    ],
+    "macOS": {
+      "minimumSystemVersion": "12.0"
+    }
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
diff --git a/pegasusX/apps/supplier-portal/src-tauri/tauri.microsoftstore.conf.json b/pegasusX/apps/supplier-portal/src-tauri/tauri.microsoftstore.conf.json
new file mode 100644
index 000000000..6ef2926a1
--- /dev/null
+++ b/pegasusX/apps/supplier-portal/src-tauri/tauri.microsoftstore.conf.json
@@ -0,0 +1,16 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX",
+    "windows": {
+      "webviewInstallMode": {
+        "type": "offlineInstaller"
+      }
+    }
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
diff --git a/pegasusX/apps/supplier-portal/src-tauri/tauri.store.conf.json b/pegasusX/apps/supplier-portal/src-tauri/tauri.store.conf.json
new file mode 100644
index 000000000..9af34067b
--- /dev/null
+++ b/pegasusX/apps/supplier-portal/src-tauri/tauri.store.conf.json
@@ -0,0 +1,11 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX"
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
diff --git a/pegasusX/apps/warehouse-portal/app/orders/[id]/page.tsx b/pegasusX/apps/warehouse-portal/app/orders/[id]/page.tsx
index 679f6e625..f0b958c58 100644
--- a/pegasusX/apps/warehouse-portal/app/orders/[id]/page.tsx
+++ b/pegasusX/apps/warehouse-portal/app/orders/[id]/page.tsx
@@ -4,7 +4,7 @@ import { useCallback, useEffect, useState } from 'react';
 import { useParams, useRouter, useSearchParams } from 'next/navigation';
 import Link from 'next/link';
 import type { WarehouseOrderDetail } from '@pegasusx/types';
-import { ApiError } from '@pegasusx/api-client';
+import { adminForceCompleteKey, ApiError } from '@pegasusx/api-client';
 import Icon from '@/components/Icon';
 import { OrderTimelinePanel } from '@/components/OrderTimelinePanel';
 import PageTransition from '@/components/PageTransition';
@@ -33,6 +33,7 @@ export default function OrderDetailPage() {
   const [loading, setLoading] = useState(true);
   const [acting, setActing] = useState(false);
   const [reason, setReason] = useState('');
+  const [forceReason, setForceReason] = useState('OFD_DOWN');
   const [proposedDate, setProposedDate] = useState(() => new Date().toISOString().slice(0, 10));
 
   const load = useCallback(async () => {
@@ -71,6 +72,8 @@ export default function OrderDetailPage() {
 
   const state = (order?.state ?? order?.status ?? '').toUpperCase();
   const flags = orderActionFlags(state);
+  const canForceFiscal =
+    state === 'FISCAL_FAILED' || state === 'FISCALIZING';
   const total = order?.total_uzs ?? order?.total_minor ?? 0;
   const fmt = (n: number) => new Intl.NumberFormat('uz-UZ').format(n);
 
@@ -145,15 +148,59 @@ export default function OrderDetailPage() {
               </div>
             </section>
 
-            {showOps ? (
+            {showOps || canForceFiscal ? (
               <aside className="wh-bay-panel wh-bay--ops wh-order-bento-ops">
                 <div className="wh-section-head">
                   <div>
                     <h2 className="wh-section-title">Quick actions</h2>
-                    <p className="wh-section-desc">Propose a new date or cancel. Retailer is notified.</p>
+                    <p className="wh-section-desc">
+                      {canForceFiscal
+                        ? 'Fiscal hard-gate exception or logistics actions.'
+                        : 'Propose a new date or cancel. Retailer is notified.'}
+                    </p>
                   </div>
                 </div>
                 <div className="p-5 space-y-4">
+                  {canForceFiscal ? (
+                    <div className="space-y-2 rounded-xl border border-[var(--desk-warning)]/30 bg-[var(--desk-warning)]/10 p-3">
+                      <p className="text-xs font-medium uppercase tracking-wider text-[var(--desk-warning)]">
+                        Force-complete (fiscal)
+                      </p>
+                      <p className="text-xs text-[var(--muted)]">
+                        Audited skip when OFD is stuck. Requires reason code.
+                      </p>
+                      <select
+                        className="portal-input"
+                        value={forceReason}
+                        onChange={(e) => setForceReason(e.target.value)}
+                        disabled={acting}
+                      >
+                        {['OFD_DOWN', 'OFD_TIMEOUT', 'OPS_ESCALATION', 'TAX_EXEMPT_POLICY', 'OTHER'].map(
+                          (code) => (
+                            <option key={code} value={code}>
+                              {code}
+                            </option>
+                          ),
+                        )}
+                      </select>
+                      <button
+                        type="button"
+                        disabled={acting || !forceReason}
+                        className="portal-btn portal-btn--primary w-full"
+                        onClick={() =>
+                          runMutation('Force-completed (fiscal skipped)', () =>
+                            warehouseApi.forceCompleteOrder(
+                              orderId,
+                              { reason_code: forceReason },
+                              adminForceCompleteKey(orderId, forceReason),
+                            ),
+                          )
+                        }
+                      >
+                        Force complete
+                      </button>
+                    </div>
+                  ) : null}
                   {flags.canDelay ? (
                     <label className="portal-field">
                       <span className="portal-label">Proposed delivery date</span>
diff --git a/pegasusX/apps/warehouse-portal/components/ClientPolicyBanner.tsx b/pegasusX/apps/warehouse-portal/components/ClientPolicyBanner.tsx
index 8cc4b2540..ad95bb847 100644
--- a/pegasusX/apps/warehouse-portal/components/ClientPolicyBanner.tsx
+++ b/pegasusX/apps/warehouse-portal/components/ClientPolicyBanner.tsx
@@ -2,6 +2,7 @@
 
 import { useEffect, useState } from "react";
 import { AlertTriangle } from "lucide-react";
+import { desktopClientPolicyContext } from "@pegasusx/desktop-bridge";
 import { getClientPolicy } from "@/lib/client-policy";
 
 type PolicyState = {
@@ -17,8 +18,10 @@ export default function ClientPolicyBanner() {
 
   useEffect(() => {
     const version =
-      process.env.NEXT_PUBLIC_WAREHOUSE_APP_VERSION?.trim() || "1.0.0";
-    void getClientPolicy("web", version)
+      process.env.NEXT_PUBLIC_WAREHOUSE_APP_VERSION?.trim() || "0.1.0";
+    // Tauri: enterprise → website CDN; store → production (MS/Mac App Store).
+    const { platform, channel } = desktopClientPolicyContext();
+    void getClientPolicy(platform, version, channel)
       .then(async (res) => {
         if (!res.ok) return;
         const data = (await res.json()) as {
diff --git a/pegasusX/apps/warehouse-portal/components/Providers.tsx b/pegasusX/apps/warehouse-portal/components/Providers.tsx
index c53c8f9ef..03fe8de1a 100644
--- a/pegasusX/apps/warehouse-portal/components/Providers.tsx
+++ b/pegasusX/apps/warehouse-portal/components/Providers.tsx
@@ -8,6 +8,7 @@ import { ToastProvider } from "./Toast";
 import { PageSkeleton } from "./Skeleton";
 import { DesktopCacheBootstrap } from "@/lib/desktop-cache-bootstrap";
 import { DesktopDeepLinkBootstrap } from "@/lib/desktop-deep-link";
+import { EnterpriseDesktopUpdateBootstrap } from "@/lib/desktop-updater";
 import { PortalOfflineTray } from "@/lib/portal-offline-tray";
 
 export default function Providers({ children }: { children: ReactNode }) {
@@ -16,6 +17,7 @@ export default function Providers({ children }: { children: ReactNode }) {
       <ToastProvider>
         <DesktopCacheBootstrap />
         <DesktopDeepLinkBootstrap />
+        <EnterpriseDesktopUpdateBootstrap />
         <div className="app-root min-h-screen w-full">
           <WarehouseShell>
             <Suspense fallback={<PageSkeleton />}>
diff --git a/pegasusX/apps/warehouse-portal/lib/order-actions.ts b/pegasusX/apps/warehouse-portal/lib/order-actions.ts
index e24e33e11..429083e27 100644
--- a/pegasusX/apps/warehouse-portal/lib/order-actions.ts
+++ b/pegasusX/apps/warehouse-portal/lib/order-actions.ts
@@ -31,6 +31,8 @@ export const ORDER_STATE_CHIP: Record<string, string> = {
   DELAYED: 'status-chip--draft',
   ARRIVED: 'status-chip--ready',
   COMPLETED: 'status-chip--stable',
+  FISCALIZING: 'status-chip--warning',
+  FISCAL_FAILED: 'status-chip--danger',
   CANCELLED: 'status-chip--critical',
   SCHEDULED: 'status-chip--draft',
   AUTO_ACCEPTED: 'status-chip--ready',
diff --git a/pegasusX/apps/warehouse-portal/package.json b/pegasusX/apps/warehouse-portal/package.json
index 2cfee119a..30e3828d2 100644
--- a/pegasusX/apps/warehouse-portal/package.json
+++ b/pegasusX/apps/warehouse-portal/package.json
@@ -15,7 +15,12 @@
     "tauri:build": "TAURI_BUILD=1 tauri build",
     "tauri:build:mac": "TAURI_BUILD=1 tauri build --target universal-apple-darwin",
     "tauri:build:win": "TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc",
-    "tauri:build:debug": "TAURI_BUILD=1 tauri build --debug"
+    "tauri:build:debug": "TAURI_BUILD=1 tauri build --debug",
+    "tauri:build:store": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --config src-tauri/tauri.store.conf.json",
+    "tauri:build:store:win": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc --config src-tauri/tauri.microsoftstore.conf.json",
+    "tauri:build:store:mac": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=store TAURI_BUILD=1 tauri build --target universal-apple-darwin --config src-tauri/tauri.macappstore.conf.json",
+    "tauri:build:enterprise:win": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=enterprise TAURI_BUILD=1 tauri build --target x86_64-pc-windows-msvc",
+    "tauri:build:enterprise:mac": "NEXT_PUBLIC_DESKTOP_DISTRIBUTION=enterprise TAURI_BUILD=1 tauri build --target universal-apple-darwin"
   },
   "dependencies": {
     "@heroui/react": "^3.1.0",
@@ -32,7 +37,9 @@
     "@tauri-apps/plugin-dialog": "^2.7.0",
     "@tauri-apps/plugin-fs": "^2.5.0",
     "@tauri-apps/plugin-deep-link": "^2.4.9",
+    "@tauri-apps/plugin-process": "^2.3.1",
     "@tauri-apps/plugin-sql": "^2.2.0",
+    "@tauri-apps/plugin-updater": "^2.9.0",
     "firebase": "^12.12.0",
     "framer-motion": "^12.40.0",
     "h3-js": "^4.5.0",
diff --git a/pegasusX/apps/warehouse-portal/src-tauri/Cargo.toml b/pegasusX/apps/warehouse-portal/src-tauri/Cargo.toml
index 412ac12fa..a808ce332 100644
--- a/pegasusX/apps/warehouse-portal/src-tauri/Cargo.toml
+++ b/pegasusX/apps/warehouse-portal/src-tauri/Cargo.toml
@@ -24,6 +24,7 @@ tauri-plugin-sql = { version = "2", features = ["sqlite"] }
 [target.'cfg(not(any(target_os = "android", target_os = "ios")))'.dependencies]
 tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }
 tauri-plugin-deep-link = "2"
+tauri-plugin-updater = "2"
 serde = { version = "1", features = ["derive"] }
 serde_json = "1"
 keyring = { version = "3", features = ["apple-native", "windows-native"] }
diff --git a/pegasusX/apps/warehouse-portal/src-tauri/capabilities/default.json b/pegasusX/apps/warehouse-portal/src-tauri/capabilities/default.json
index 09d97c809..2dd8057dd 100644
--- a/pegasusX/apps/warehouse-portal/src-tauri/capabilities/default.json
+++ b/pegasusX/apps/warehouse-portal/src-tauri/capabilities/default.json
@@ -26,6 +26,7 @@
     "process:default",
     "process:allow-exit",
     "process:allow-restart",
-    "sql:default"
+    "sql:default",
+    "updater:default"
   ]
 }
diff --git a/pegasusX/apps/warehouse-portal/src-tauri/src/lib.rs b/pegasusX/apps/warehouse-portal/src-tauri/src/lib.rs
index 5bbdada9a..f7d1cd005 100644
--- a/pegasusX/apps/warehouse-portal/src-tauri/src/lib.rs
+++ b/pegasusX/apps/warehouse-portal/src-tauri/src/lib.rs
@@ -22,7 +22,8 @@ pub fn run() {
                     let _ = window.set_focus();
                 }
             }))
-            .plugin(tauri_plugin_deep_link::init());
+            .plugin(tauri_plugin_deep_link::init())
+            .plugin(tauri_plugin_updater::Builder::new().build());
     }
 
     builder
diff --git a/pegasusX/apps/warehouse-portal/src-tauri/tauri.conf.json b/pegasusX/apps/warehouse-portal/src-tauri/tauri.conf.json
index 4ceb1191a..56fd8652d 100644
--- a/pegasusX/apps/warehouse-portal/src-tauri/tauri.conf.json
+++ b/pegasusX/apps/warehouse-portal/src-tauri/tauri.conf.json
@@ -31,19 +31,12 @@
     "security": {
       "dangerousDisableAssetCspModification": false,
       "csp": "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; img-src 'self' data: blob:; connect-src 'self' http://localhost:8180 http://127.0.0.1:8180 http://localhost:9099 http://127.0.0.1:9099 ws://localhost:8180 wss://localhost:8180"
-    },
-    "updater": {
-      "active": true,
-      "endpoints": [
-        "https://storage.googleapis.com/pegasusx-ssmr-app-updates/warehouse-desktop/{{target}}/{{arch}}/updater.json"
-      ],
-      "dialog": true,
-      "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDhDREMyMEEzRkY5NEYzQjMKUldTejg1VC9veURjakZ6R3NoSmZ6NGIyOGVBZVBZZGs5TkdMWk9XRTArQ2UyakJNZ01XYXZtS0UK"
     }
   },
   "bundle": {
     "active": true,
     "targets": "all",
+    "createUpdaterArtifacts": true,
     "icon": [
       "icons/32x32.png",
       "icons/128x128.png",
@@ -64,6 +57,15 @@
       "desktop": {
         "schemes": ["pegasusx-warehouse"]
       }
+    },
+    "updater": {
+      "pubkey": "dW50cnVzdGVkIGNvbW1lbnQ6IG1pbmlzaWduIHB1YmxpYyBrZXk6IDhDREMyMEEzRkY5NEYzQjMKUldTejg1VC9veURjakZ6R3NoSmZ6NGIyOGVBZVBZZGs5TkdMWk9XRTArQ2UyakJNZ01XYXZtS0UK",
+      "endpoints": [
+        "https://storage.googleapis.com/pegasusx-ssmr-app-updates/warehouse-desktop/{{target}}/{{arch}}/updater.json"
+      ],
+      "windows": {
+        "installMode": "passive"
+      }
     }
   }
 }
diff --git a/pegasusX/apps/warehouse-portal/src-tauri/tauri.macappstore.conf.json b/pegasusX/apps/warehouse-portal/src-tauri/tauri.macappstore.conf.json
new file mode 100644
index 000000000..78f357c8f
--- /dev/null
+++ b/pegasusX/apps/warehouse-portal/src-tauri/tauri.macappstore.conf.json
@@ -0,0 +1,18 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX",
+    "targets": [
+      "app",
+      "dmg"
+    ],
+    "macOS": {
+      "minimumSystemVersion": "12.0"
+    }
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
diff --git a/pegasusX/apps/warehouse-portal/src-tauri/tauri.microsoftstore.conf.json b/pegasusX/apps/warehouse-portal/src-tauri/tauri.microsoftstore.conf.json
new file mode 100644
index 000000000..6ef2926a1
--- /dev/null
+++ b/pegasusX/apps/warehouse-portal/src-tauri/tauri.microsoftstore.conf.json
@@ -0,0 +1,16 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX",
+    "windows": {
+      "webviewInstallMode": {
+        "type": "offlineInstaller"
+      }
+    }
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
diff --git a/pegasusX/apps/warehouse-portal/src-tauri/tauri.store.conf.json b/pegasusX/apps/warehouse-portal/src-tauri/tauri.store.conf.json
new file mode 100644
index 000000000..9af34067b
--- /dev/null
+++ b/pegasusX/apps/warehouse-portal/src-tauri/tauri.store.conf.json
@@ -0,0 +1,11 @@
+{
+  "bundle": {
+    "createUpdaterArtifacts": false,
+    "publisher": "PegasusX"
+  },
+  "plugins": {
+    "updater": {
+      "endpoints": []
+    }
+  }
+}
