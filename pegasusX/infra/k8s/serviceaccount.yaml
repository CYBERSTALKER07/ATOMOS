apiVersion: v1
kind: ServiceAccount
metadata:
  name: backend-go
  namespace: pegasusx
  labels:
    app: backend-go
    app.kubernetes.io/part-of: pegasusx
  annotations:
    # Replace with terraform output backend_runtime_service_account after apply.
    iam.gke.io/gcp-service-account: staging-backend@pegasus-503013.iam.gserviceaccount.com
