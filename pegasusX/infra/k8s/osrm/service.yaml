apiVersion: v1
kind: Service
metadata:
  name: osrm
  labels:
    app: osrm
    app.kubernetes.io/part-of: pegasusx
spec:
  selector:
    app: osrm
  ports:
    - name: http
      port: 5000
      targetPort: http
