apiVersion: v1
kind: ConfigMap
metadata:
  name: optimizer-core-config
  labels:
    app: optimizer-core
    app.kubernetes.io/name: optimizer-core
data:
  OPTIMIZER_HTTP_PORT: "8082"
  # Soft HTTP/handler budget; contract tunables.time_limit_ms defaults to 5000.
  OPTIMIZER_SOFT_TIMEOUT_SEC: "8"
