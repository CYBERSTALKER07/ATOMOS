apiVersion: v1
kind: ConfigMap
metadata:
  name: optimizer-core-config
  labels:
    app: optimizer-core
    app.kubernetes.io/name: optimizer-core
data:
  OPTIMIZER_HTTP_PORT: "8082"
  OPTIMIZER_SOFT_TIMEOUT_SEC: "2"
