apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pegasusx-api
  annotations:
    networking.gke.io/managed-certificates: pegasusx-api-cert
spec:
  # Prefer ManagedCertificate; keep tls host for GCE ingress host matching.
  tls:
    - hosts:
        - api.pegasusx.app
