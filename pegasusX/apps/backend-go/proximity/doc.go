// Package proximity holds geospatial helpers shared across retailer and dispatch
// domains. Receiving-window validation lives here because dispatch SLA solvers
// consume the same HH:MM wire shape as retailer profile mutations.
package proximity
