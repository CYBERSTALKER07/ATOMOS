// Package returns owns the physical reverse-logistics gate workflow:
// driver-rejected goods arrive at the warehouse, are scanned by EAN, and are
// restocked or written off with inventory credit.
package returns
