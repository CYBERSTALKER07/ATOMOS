// Package storage owns Google Cloud Storage helpers for supplier catalog image
// uploads. Signed upload tickets are issued to authenticated suppliers; the
// package does not perform HTTP handler auth itself.
package storage
