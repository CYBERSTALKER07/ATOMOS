<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    Merged with the auto-generated Info.plist (GENERATE_INFOPLIST_FILE = YES).
    Only delta keys live here — everything else comes from INFOPLIST_KEY_* build settings.
    NSAllowsLocalNetworking permits cleartext HTTP to localhost and LAN IPs
    for local development. It does NOT allow arbitrary cleartext to the internet.
-->
<plist version="1.0">
<dict>
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
</dict>
</plist>
