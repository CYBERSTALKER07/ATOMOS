# retailer-app-android

Kotlin + Jetpack Compose Material 3 retailer app. JWT role `RETAILER`. Live channel: `/ws/retailer`. FCM fallback mapping must include `SETTLEMENT_REQUIRED` and `DELIVERY_SESSION_UPDATED`.
