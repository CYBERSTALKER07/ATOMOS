# driver-app-android

Kotlin + Jetpack Compose Material 3 driver app. JWT role `DRIVER`, scoped to a `HomeNode`. Live channel: `/ws/driver`. Sends best-effort command ACKs (`sv=2`).

To scaffold: `android-studio` → New Project → Empty Compose Activity → set `applicationId` `com.pegasusx.driver`.
