package com.pegasusx.driver.data.remote

/** Shown when a reconnect clears an in-flight mutation spinner. */
const val DRIVER_RECONNECT_RECOVERY_HINT =
    "Connection restored — verify status before retrying."
