export function generateStaticParams() {
  return [{ id: "placeholder" }];
}

export default function SupplyRequestDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
