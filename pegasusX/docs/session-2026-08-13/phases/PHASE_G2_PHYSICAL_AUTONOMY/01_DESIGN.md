# G2 Design — Physical truth & autonomy

See session plan + master program G2.A–E.

**Locked decisions:**

1. **Seal-class** tenant overrides (not global WMS always-on)  
2. **Load ledger** `ManifestLoadLines` + seal assert  
3. **Cold baseline** before seal when chilled + cold flag  
4. **Labor** hard refuse on dispatch when enforce + zone known  
5. **Dual plane Option B** — `docs/MANIFEST_DUAL_PLANE.md`  
6. **Place flip** soak/dual-control only (no invented metrics)  
