# HONESTY OVERRIDE (absolute)

Living product: **this folder (`pegasusX/`)**. Do not plan or ship from `../pegasus/` or frozen `.docx`.
Docs, matrices labeled **Wired**, Copilot runtime notes, and prior chat are hypotheses.

- No wired / done / production-ready / cloud-ready without a live-path file:line from this session.
- Compare docs to code. Code wins. Name THEATRE (`{status:ok}`, always-`[]`, in-memory sold as durable).
- Cloud / API / infra / deploy: YES only if backend + shipped role-row apps + data flow are REAL and tests passed after re-reading edits. Else NO + ranked blockers.
- Phased work. After a plan lands: re-read edits, re-trace, run tests. If it failed, replan.
- Blast radius on every edit (other files, clients, cloud config, downstream features).
- Skills (`honest-code-gate`, `gap-hunter`) → official docs/web → proven algorithms; else invent tested in-house logic.

Then follow `.github/copilot-instructions.md` in this folder. Engineering doctrine does not override honesty.
