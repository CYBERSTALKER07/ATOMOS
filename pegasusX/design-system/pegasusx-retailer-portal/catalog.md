# Catalog Page Overrides

- `PageChrome` with `icon="catalog"`, `skeletonVariant="catalog"`.
- Product grid inside `PageSection`; category filter chips.
- `ProductDetailDrawer` actions use `portal-btn`.
