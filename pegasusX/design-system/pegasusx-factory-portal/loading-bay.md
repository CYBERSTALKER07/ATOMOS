# Loading Bay Page Overrides

- `PageChrome` with `icon="loadingBay"` and `skeletonVariant="table"`.
- Operational tables wrapped in `desk-table-wrap`.
- Primary actions use `portal-btn portal-btn--primary`.
- Status chips use existing `status-chip` tokens; ensure 4.5:1 contrast in light mode.
